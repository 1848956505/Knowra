# 第二阶段规范符合性矩阵

基线：B2。条款定义和来源见[规范清单](02-规范清单与适用范围.md)。本表是第二阶段增量结果，第一阶段表保留为当时的计划，不倒改历史状态。

“符合（静态）”只针对本行描述的静态证据；“部分核验”表示已有结构证据，但不足以完整判定；“偏离”指存在可定位的当前问题；“待核验”未形成判断；“不适用”仅指本次审查没有触发该工作流。

## 1. N-01～35 核验

| 条款 | 本阶段状态 | 证据与判断边界 | 后续工作 |
| --- | --- | --- | --- |
| N-01 中文文案/英文标识符 | 部分核验 | 人工读取的现行 UI/报告主要为中文，标识符为英文；未审全量文案与历史提交 | 模块阅读时检查错误/空态文案及命名一致性 |
| N-02 workspace/工具版本 | 部分核验 | 根 workspaces、Node/npm 下限、六个清单与锁元数据一致；B1 工具版本满足下限 | 隔离 npm ci、Node 24 与跨平台执行 |
| N-03 V4 唯一启动 | 符合（静态） | 根及 workspace scripts、start-dev、PM2 均为 API+V4；V3 仅测试入口 | 生产构建/启动实测 |
| N-04 V4/V3 隔离 | 符合（静态） | 原有门禁及补充字面量依赖扫描均无违规 | 动态资源、打包产物及运行路径验证 |
| N-05 React Aria 包装 | 符合（静态） | 直接包和子路径扫描均位于 components/ui 范围 | 交互语义与控件复用质量 |
| N-06 web-core 无框架/DOM/CSS | 符合（静态） | 现有门禁与依赖扫描通过，源码无外部包导入；类型/构建未运行 | 公共契约语义和运行测试 |
| N-07 职责分层 | 部分核验 | 有物理分层和注入接口；无选定逆向导入；根基础设施复用纯规则已明确登记 | S2-05 职责集中及纯规则归属，逐模块语义审查 |
| N-08 文件职责规模 | 改进建议 | 41 个 >250 行提示，12 个 >400 行；仅 NoteEditorView/workspaceSlice 确认多类职责集中 | S2-05；保留图标/Schema/迁移等例外，不机械拆文件 |
| N-09 tokens/集中配置 | 部分核验，配置有偏离 | CSS Modules 和三份全局样式已登记；PM2 API 端口有两个不联动来源 | S2-01；样式值是否合理复用和配置消费需深入 |
| N-10 样式与状态分离 | 部分核验 | V4 32 份 Modules、3 份全局 CSS；Store 与局部 UI 状态分开 | 组件具体状态所有权和复用判断 |
| N-11 单 Store/slices/selectors | 部分核验 | createAppStore 组合四个 slice；页面使用 selectors；未将整个 Store 作为 App prop 下传 | 广订阅、重渲染和加载并发专项 |
| N-12 React/Milkdown 所有权 | 部分核验 | typed command adapter、独立宿主、懒加载入口存在；原有禁用 DOM API 门禁通过 | 生命周期、焦点、选区和多笔记切换 |
| N-13 外壳与独立滚动 | 待核验 | 样式和组件路径已定位；没有运行页面测量 | 多面板、窄屏、内容长度及实际滚动验证 |
| N-14 项目弹窗 | 部分核验 | 所扫描 V4/web-core 非测试文本未命中 alert/confirm/prompt 调用，业务使用 Dialog | 所有确认/取消和键盘焦点流程 |
| N-15 当前视觉参考优先 | 待核验 | 参考已登记；未加载截图比较，也未裁定图标不分页的产品意图 | 当前任务参考、tokens、状态视觉验证 |
| N-16 API 分层与运行框架 | 符合（静态入口） | node:http server、factory、HTTP handlers、application/repository；NestJS 仅清单预留 | 路由内业务侵入、服务与持久化职责逐项阅读 |
| N-17 信封/DTO 契约 | 部分核验 | server/response 使用成功和失败信封，共享 API 模块存在 | 各路由 DTO/状态码/错误/非法输入对照 |
| N-18 驱动显式选择 | 部分核验 | factory 分支选择 postgres；Prisma runtime 缺连接配置/连接失败抛错，不见 catch 后切 JSON | 驱动配置非法值、实库不可用与失败重启 |
| N-19 存储路径配置 | 待核验 | uploads/env config 已定位；exports/temp/mode 的声明与实际消费尚未完整核验 | 配置到读写入口追踪，隔离路径验证 |
| N-20 JSON 原子/引用/删除事务 | 部分核验 | file-data-store、atomic-json、relations 及 note-deletion runTransaction 接线存在 | 故障注入、整批预检、磁盘/内存回滚与重启 |
| N-21 PostgreSQL 事务/外部 IO | 待核验 | 事务/快照/文件型协调已归属，未逐个事务读取/实测 | 所有事务闭包和补偿路径 |
| N-22 批量/并发/恢复 | 待核验；有性能风险 | useNotesIndexData 存在逐批拉全结构，S2-06；数据库批量/并发尚未验证 | 大数据请求/查询数及维护操作并发 |
| N-23 owner/认证 | 部分核验 | factory 注入 owner；Nginx 模板全站 Basic Auth、health 匿名例外 | HTTP 越界负测；线上状态未核验 |
| N-24 cache/loading 写保护 | 部分核验 | workspace mutation 要求 dataMode=api；cache→live、只读失败路径存在 | 保存失败离开、旧响应、缓存损坏等运行证据 |
| N-25 端口发现与代理 | 偏离 | 默认路径合理，但 PM2 自定义 API 端口与 Web API_ORIGIN 不联动，S2-01 | 修复后纯配置断言与隔离 HTTP 验证 |
| N-26 附件路径/主动内容 | 部分核验 | 二进制响应含 disposition、sandbox、nosniff；路径模块已定位 | 全部读取/删除/导入/迁移负测及路径别名 |
| N-27 正式资产/版本 | 待核验 | Schema v4、规则复用、服务与仓储接线可定位；不能据结构证明一致性 | 各实体确认、引用失效、删除、快照/迁移入口对照 |
| N-28 双驱动一致 | 待核验 | 同步/异步两组服务和 repository 存在；不是重复实现缺陷的直接证据 | 同一 fixture 对照与真实 PostgreSQL |
| N-29 测试实际执行机制 | 部分核验 | API 33 个测试文件均导入并数组执行；V3 动态发现；Vitest/服务器/E2E 分开 | 断言、异步完成、skip/fixture、运行数；CI 门禁缺口另见 S2-02 |
| N-30 跨平台工具/LF | 部分核验 | 核心脚本 Node path；post-deploy Bash 有 shebang/set -euo pipefail/PM2 探测；所扫源码配置无 CRLF | Windows/macOS/Linux 与命令缺失、错误退出场景 |
| N-31 保留改动/upstream | 符合（本次过程） | 按 B1/B2 指纹保留用户原有变更；不同步、暂存、提交或重置 | 后续开始前继续比对；远端结论仍未核验 |
| N-32 文档与结构导航 | 偏离 | 附件/分页现状描述过时，当前 V4 导航有失效链接；S2-03/04 | 文档整改与链接核验；本轮只记问题 |
| N-33 SemVer/lock 清单同步 | 符合（静态元数据） | 四主应用 2.20.1；六清单版本与根 lock package 元数据一致；共享包版本独立合法 | 可复现安装和下一次发版过程 |
| N-34 提交/tag 工作流 | 本次不适用 | 没有提交、tag、发版或改写历史 | 后续产生提交时按规范执行 |
| N-35 源码/数据/原型边界 | 部分核验 | 根 apps/packages/scripts/docs/deploy 分区明确；.gitignore 覆盖主要数据与产物；shared 为占位 | 忽略文件族、间接资源引用和遗留退役策略 |

## 2. 补充质量目标状态

| 目标 | 第二阶段进度 | 未完成部分 |
| --- | --- | --- |
| Q-01 功能正确性 | 确认入口和 D-01～07 的当前实现结构 | 正/负面行为与状态规则 |
| Q-02 竞态/生命周期 | 标记保存、加载、编辑器与维护协调热点 | 异步竞态和资源释放复现 |
| Q-03 可恢复性 | 定位 JSON/PG/附件/快照边界 | 备份恢复、故障注入、重启 |
| Q-04 安全/供应链 | 确认模板认证边界、源码依赖范围、锁元数据 | 漏洞扫描、可达性/利用条件、生产现状 |
| Q-05 可扩展性 | 依赖图与职责热点、纯规则跨层复用例外 | 逐模块变更影响推演 |
| Q-06 维护性 | 全文件归属、循环/完全重复扫描、热点和文档偏差 | 语义重复、精确死代码与遗留清理成本 |
| Q-07 性能 | S2-06 全量拉取结构风险 | 定义规模与阈值后测量 |
| Q-08 测试有效性 | runner/CI 接线；S2-02 门禁缺口 | 断言质量与实际执行 |
| Q-09 可访问性 | UI 包装边界与组件入口 | 键盘/焦点/屏幕阅读/缩放/IME |
| Q-10 构建运维 | S2-01 配置推演、CI/部署链图 | 安装、构建、启动、健康检查与回退演练 |

没有将待核验项计为通过，也没有按扫描文件数计算质量评分。

# Knowra 全项目工程质量审查总报告

审查日期：2026-09-05～2026-09-06。范围：整个前后端及共享包、脚本、部署、测试、文档、遗留与产物边界。最终基线：KNOWRA-REVIEW-20260906-B6；本地提交 `22231a28b27bba3fb0b0f265e4bd28d505a754dc`，包含审查开始前已有未提交改动。远端最新状态未核验。

**六阶段审查资料已完成。当前有 16 项已证实问题（5 项 P1、9 项 P2、2 项 P3）、2 项风险、1 项维护建议；所有问题尚未修复。不能据此宣称项目质量全部达标。** 真实 PostgreSQL、联网依赖漏洞扫描、跨平台等缺口已明确登记，未当作通过。

## 1. 对主要审查目标的结论

| 目标 | 总体判断 | 主要依据 |
| --- | --- | --- |
| 是否符合 docs 开发规范 | 核心架构边界有实现并通过本机门禁；数据一致性、契约完整性、测试/文档同步仍有偏离 | 35条规范逐项有状态；R-01～16；不计算无依据的合规百分比 |
| 健壮性与数据恢复 | 正常保存/冲突拒绝、附件保护、部分导入回滚可工作；失败和中断路径存在5项P1，需优先整改 | JSON事务边界、草稿生命周期、UTF-8分块、PG模型清单、备用替换恢复 |
| 可扩展性 | 分层可追踪；新模型/参数依赖多个手写清单，扩展后容易漏接 | tagGroups在缓存/快照/初始化、新查询字段在搜索路径遗漏；建议共享契约和覆盖断言 |
| 目录结构与维护 | 根目录与业务分层总体清晰，未发现需要大规模目录重排的证据 | apps/packages/scripts/deploy/docs职责可定位；无选定静态循环/越界；R-19属局部职责建议 |
| 功能正确性 | 已覆盖关键本地正常和失败流程，存在明确边界错误 | 恢复重名、分组重复ID、搜索筛选、新空间标签等；并非所有实体/组合全分支验收 |
| 安全与供应链 | 路径、主动附件内容、软链和输入负测样本有效；不能判定安全全覆盖 | R-11请求限制；漏洞联网扫描未执行；线上认证未核验 |
| 性能与容量 | 工作量随总数据线性放大已有定量证据，需先约定目标容量与SLO | 1万篇334页请求、1万图标节点；R-17保留容量风险，不凭单次耗时宣称违反既定阈值 |
| 测试与可运维性 | 标准检查和本机生产构建通过；完整浏览器回归仍有旧测试与稳定性问题 | 500个可计数用例、151遗留测试文件；原E2E43/48；配置/CI门禁缺口R-12～14 |

项目已有的分层和保护机制值得保留。修复应先补齐业务提交、状态恢复和模型契约，再做容量设计与职责提取；不宜为了“目录整齐”整体重写或机械拆分大文件。

## 2. 最终问题规模与优先级

20条历史记录合并为19条。S3-01的JSON失败写入与S4-03的批量导入部分提交合为R-01，保留两个场景的独立验收要求。其余保持原始编号映射，不把同属标签领域的不同根因强行合并。

| 类别 | 数量 | 解释 |
| --- | --- | --- |
| P1 已证实问题 | 5 | R-01 JSON提交边界；R-02冲突草稿；R-03UTF-8分块；R-04PG快照/迁移字段；R-05备用替换中断恢复 |
| P2 已证实问题 | 9 | R-06～14，含缓存/标签/恢复/搜索/初始化/请求错误、部署配置与测试门禁 |
| P3 已证实问题 | 2 | R-15～16，文档状态与失效引用 |
| 风险 | 2 | R-17容量已量化但未约定SLO；R-18写分类遗漏已确认，但实库并发结果未知 |
| 维护建议 | 1 | R-19编辑器和workspace slice职责集中，不按行数直接定bug |

未发现经证实的P0；这不是已经排除所有P0的保证。R-04的证据是实际函数契约与模型校验，未在真实数据库运行。R-05通过真实子进程SIGKILL验证，但Windows替换错误由适配器模拟；旧备份仍在，不能描述成磁盘数据永久擦除。

完整触发条件、位置、建议、验收与证据见 [18-最终问题清单](18-最终问题清单.md)。

## 3. 实际覆盖与证据

- 全仓文件归属与结构扫描，18个审查单元均有明确覆盖状态。第三阶段对关键路径深入阅读，低风险/遗留部分使用代表性核验，不宣称所有自有文件逐行语义审查。
- 35条规范、10项补充质量目标、9条跨层链路均登记最终状态和缺口，见 [19-最终规范与覆盖矩阵](19-最终规范与覆盖矩阵.md)。
- 第四阶段运行15个真实HTTP场景与10个实际V4浏览器场景：14项符合该场景断言，11项成功复现缺陷。该计数不是产品通过率。
- 第五阶段标准检查通过：API214、web-core19、V4 Vitest248、生产服务2、脚本17，共500项；V3另为151个测试文件，未混称独立用例。
- 完整E2E48项首次43通过、5失败；其中4项旧断言/缺失mock在审查副本纠正后通过，原测试未改。剩余表格项串行原用例及两个真实API页面样本通过，仍保留首次失败和待观察项。
- 本机生产构建、Prisma schema校验、实际生产静态服务/代理的中文保存重载通过；另测20并发保存、请求/附件安全、缺失附件、进程中断和三档容量。

详细命令、环境、计数及测量见 [15-第五阶段测试与测量记录](15-第五阶段测试与测量记录.md)。完整首轮失败、诊断副本、trace和截图均保留；未以复跑覆盖失败或把缺陷复现脚本的退出0当作产品正常。

## 4. 明确的审查限制

| 缺口 | 已做的部分 | 尚不能得出的结论 |
| --- | --- | --- |
| 真实PostgreSQL | 源码、契约、mock、schema校验 | 事务隔离、真实外键、迁移恢复与多进程并发全部通过 |
| 依赖漏洞联网扫描 | lockfile离线清点；npm audit尝试失败 | 零已知漏洞、生产/开发依赖均安全 |
| 跨平台/最低版本 | macOS arm64、Node26、Chromium | Windows/Linux、Node24、Safari/Firefox和OS输入法通过 |
| 深度恢复/压力 | 有限写入故障、3个进程中断点、20并发请求、三档规模 | 掉电/磁盘损坏、所有IO时序、稳态容量或泄漏全部排除 |
| 发布与外部环境 | 仓库模板、脚本、本地生产冒烟 | 线上认证、PM2回退、真实数据恢复、干净安装已验证 |
| 阅读深度 | 全范围归属+关键路径深入阅读 | 整个仓库所有分支、所有历史文件逐行验收 |

联网audit需向npm registry提交依赖名称/版本，自动批准审查因缺少该具体外发授权而拒绝；本轮未将“完成最后阶段”解释成授权该数据外发，也未绕过。详见阶段五限制及 [整改路线图](20-整改路线图与验收门槛.md)。

## 5. 第六阶段复核与交付

B6对比B5，业务源码与原测试/截图无漂移；变化只来自第五阶段审查资料及导航/日志。第六阶段复查配置/CI、分组创建、PG快照等关键定位，核对所有历史编号、合并关系、结果计数、矩阵覆盖和文档链接。未在相同源码上无理由重跑整套测试，运行结论按B5环境承接。最终再与最初B1比对，原有业务源码、测试和截图无变动；108个最终文档本地链接检查无失效，见 [收尾核验](证据/阶段6/收尾核验.json)。

对照方案的五项交付：

| 交付 | 当前入口 |
| --- | --- |
| 总报告 | 本文件 |
| 规范/模块覆盖矩阵 | [19-最终规范与覆盖矩阵](19-最终规范与覆盖矩阵.md) |
| 可定位问题清单 | [18-最终问题清单](18-最终问题清单.md) |
| 验证证据 | [证据目录](证据/)；[第六阶段合并复核](证据/阶段6/问题合并与复核.json)；各阶段报告索引 |
| 整改路线图 | [20-整改路线图与验收门槛](20-整改路线图与验收门槛.md) |

审查交付按“所有范围有明确状态、问题可追溯、证据与缺口清晰”完成。质量整改尚未开始。没有修改业务代码、原测试、用户已有实验、依赖或真实运行数据；没有提交、发版、迁移或部署。

建议下一轮按A1～A4先处理数据与核心输入/恢复问题，并同步恢复原E2E和CI门禁；再处理标签契约、部署配置、容量与维护。具体关闭条件见路线图，不能只以总测试数变绿关闭已经复现的缺陷。

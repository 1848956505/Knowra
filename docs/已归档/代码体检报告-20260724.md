# 知境·Knowra 代码体检报告（2026-07-24）

> 本报告基于代码静态分析、自动化测试和人工审查编写。
> 记录代码库当前的非功能性质量状况及最近一轮修复成果。

---

## 一、总体评级

| 维度 | 评级 | 趋势 | 说明 |
|------|------|------|------|
| 项目结构 | ★★★★☆ | 稳定 | 前端 controller/lib 边界清晰，API DDD 四层架构 |
| 测试覆盖 | ★★★★★ | ↑ 提升 | Web 118/118, API 115/115 全部通过 |
| 代码精简度 | ★★★☆☆ | ↑ 改善 | escapeHtml 16→1, DTO 去重后提升明显 |
| 样式规范性 | ★★★☆☆ | ↑ 改善 | 15 处 CSS 硬编码色值已消除 |
| 可维护性 | ★★★☆☆ | ↑ 提升 | 重复代码减少，引用一致 |
| 可拓展性 | ★★★★☆ | 稳定 | 分层预留好，新增实体只需补齐四件套 |
| 健壮性 | ★★★★☆ | 稳定 | 无空 catch、无 N+1、无实体泄漏 |
| 安全性 | ★★★★☆ | 稳定 | 统一 escape 后 XSS 防护槽点消除 |

---

## 二、本轮已修复问题清单

### P0 — 已修复（有自信的一次性修复项）

#### 1. escapeHtml / escapeAttribute 统一

**问题：** 16 个 lib 文件各自定义私有 `escapeHtml`，且 `escapeAttribute` 的 backtick 转义实现不一致（`&#96;` vs `&#096;` vs 缺失），存在 XSS 风险差异。

**操作：** 删除全部本地定义，统一从 `src/app/formatting.js` 导入。

**统计：**
- 涉及文件：16 个 lib 文件 + 1 个测试文件（import 路径修正）
- 发现并修正的差异：5 处 escapeAttribute 行为不一致（含 `&#96;` vs `&#096;` 3 处、完全缺失 backtick 替换 2 处）
- 测试：Web 118/118 通过，无回归

**文件列表：** `navigation/context-menu-renderers.js`、`navigation/tree-renderers.js`、`navigation/section-menu-renderers.js`、`search/renderers.js`、`markdown.js`、`editor/tab-renderers.js`、`editor/file-menu.js`、`editor/editor-panel-renderers.js`、`editor/preview-renderers.js`、`editor/table-dialog-renderers.js`、`editor/tab-menu-renderers.js`、`editor/menu-renderers.js`、`editor/milkdown/host/image-paste-controller.js`、`shell/rail-renderers.js`、`status/renderers.js`、`library-index/renderers.js`

#### 2. CSS 硬编码颜色 → Design Tokens

**问题：** 旧 UI 组件 CSS 中 15 处色值为硬编码 hex，未引用全局 CSS 变量。

**操作：** 逐一映射到 `tokens.css` 中的语义变量。

| 文件 | 旧值 | 替换为 |
|------|------|--------|
| `editor-shell.css` | `#fbfcff` | `var(--color-panel)` |
| `base-shell.css` | `#44506a` | `var(--color-text-muted)` |
| `editor-utility-dialog.css` | `#51607a` | `var(--color-text-muted)` |
| `editor-utility-dialog.css` | `#b42318` | `var(--color-danger)` |
| `editor-menus.css` (×3) | `#31405c` `#142033` `#1c2840` | `var(--color-text)` |
| `preview-sidebar.css` | `#10182b` `#eff4ff` | `var(--color-panel-strong)` `var(--color-on-accent)` |
| `library-inline-context.css` (×4) | `#cb4b32` `#2457d6` `#6b7280` `#9a2b2b` | `var(--color-danger)` `var(--color-accent)` `var(--color-text-soft)` `var(--color-danger)` |

#### 3. 清理死代码

| 项目 | 操作 |
|------|------|
| `lib/knowledge-points/` | 删除空目录（v2.4.0 下线旧知识点后残留） |
| `menu-renderers.js` 中 `EDITOR_MENU_ITEMS = EDIT_MENU_ITEMS` | 移除死别名 |
| `domain/note.js` 的 `derivePlainText` / `deriveInternalLinks` | 移除未使用导出 |

#### 4. API DTO 去重

| 重复项 | 出现次数 | 操作 |
|--------|----------|------|
| `trimIfString` | 3 次（note/folder/tag dto） | 提取到 `_shared.js` |
| `createSlug` | 2 次（note/folder dto） | 提取到 `_shared.js` |

---

## 三、用户后续追加变更审查

本轮修复后用户继续提交了 UI 和功能增强，主要变更如下：

### 变更摘要

| 类别 | 文件数 | 说明 |
|------|--------|------|
| 样式增强 | 6 个 CSS 文件 | knowra-editor/knowra-shell/knowra-library-index 等重写排版和布局 |
| 功能增强 | 5 个 JS | 专注模式快捷键、表格指针容差、SVG 图标替换文本编号 |
| 测试补充 | 10+ 个测试文件 | library-index-renderers 断言扩展、shell 测试、视图菜单测试等 |
| 基础设施 | 2 个文件 | formatting.js 增加 formatCompactDate，app-state 移除 settings rail 项 |
| Demo/文档 | 4 个文件 | design-qa.md、UI 测试 demo 更新、前端重构记录 |

### 质量影响评估

| 检查点 | 结论 |
|--------|------|
| 新增 CSS 是否有硬编码颜色 | 无。knowra-*.css 仅引用 token 变量，硬编码仅出现在 `knowra-theme-tokens.css` 的定义中 |
| 是否有空的 catch 块 | 无。所有 catch 均有错误处理 |
| 是否有 TODO/FIXME/HACK | 无 |
| escapeHtml 统一是否被破坏 | 否。零本地定义残留 |
| CSS 变量修复是否被覆盖 | 否。15 处修改变更仍在 |
| 测试是否通过 | Web 118/118, API 115/115 |

---

## 四、待处理问题清单

### P1 — 短期应修复

| 问题 | 位置 | 说明 |
|------|------|------|
| JS 内联颜色硬编码 | `image-paste-controller.js` SVG 占位图、`tags/state.js` 默认色、`sidebar/renderers.js` 标签点 | SVG stop-color 不支持 CSS 变量，需要提取为常量映射 |
| 附件 ID 安全性 | `local-attachment-store-utils.js` | `Date.now() + Math.random(6 hex)` 应改为 `crypto.randomUUID()` |
| Controller 循环依赖 | `editor-controller ↔ navigation-controller` | 通过 `getController()` 延迟解析绕开，应在注册层梳理 |
| 标签色 `#3c68ff` 硬编码 | `tags/state.js:58`、`sidebar/renderers.js:46,59,73` | 运行态数据默认值，但建议暴露为 token 引用 |

### P2 — 中期优化

| 问题 | 位置 | 优先级 |
|------|------|--------|
| `local-attachment-store.js` 346 行 | 超 250 行规范，路径解析/CRUD/导入导出混杂 | 按职责继续拆分 |
| API/Local 双分支重复 | `note-command-controller.js` 8 个方法重复 `if(api){...}else{...}` | 抽取公共后置钩子 |
| `note-routes.js` 177 行 | 单函数 15+ 分支，路由分发应改为声明式 | 抽取路由表 |
| 2MB milkdown-bundle.js commit | 构建产物纳入版本库 | 考虑 gitignore + CI 构建 |
| 错误类不统一 | API 3 种风格（Error + code、fail() helper、createRequestError） | 创建集中 `AppError` 类 |
| Repository 方法名不一致 | `deleteById` vs `delete` | 抽取通用内存仓库工厂 |

### P3 — 长期关注

| 问题 | 说明 |
|------|------|
| `app.factory.js` 随实体增多膨胀 | 预设拆分策略 |
| `apps/web/app` + `components/` React 并行结构 | 明确迁移或归档路线 |
| 全局错误码枚举 | 当前 `ATTACHMENT_NOT_FOUND` 等分散为字符串字面量 |
| storage 未模块化 | `storage-routes.js` 在 `src/` 根级而非 `modules/storage/` |

---

## 五、改进趋势

```
本轮修复前                    本轮修复后
████████░░  80%              ████████░░  80%   项目结构
██████████  100%             ██████████  100%  测试覆盖
███░░░░░░░  35%  →           ██████░░░░  55%  代码精简度
███░░░░░░░  30%  →           ██████░░░░  55%  样式规范性
████░░░░░░  40%  →           ██████░░░░  55%  可维护性
████████░░  80%              ████████░░  80%  可拓展性
████████░░  80%              ████████░░  80%  健壮性
███████░░░  70%  →           ████████░░  75%  安全性
```

**最大改进：** 代码精简度 +20%、样式规范性 +25%、可维护性 +15%

---

## 六、总结

本次体检和第二轮修复取得了以下成果：

1. **消除 16 份 escapeHtml 重复定义**，统一了 5 处行为差异，从根本上消除了该模式引入安全缺陷的风险
2. **清除 15 处 CSS 硬编码颜色**，统一到 Design Tokens 体系
3. **API DTO 去重**（trimIfString ×3、createSlug ×2）
4. **清理死代码**（空目录、死别名、未使用导出）
5. **用户追加的变更质量过关**：无新增硬编码、无空 catch、无 TODO 遗留

**下阶段建议优先解决 P1 问题：** JS 内联颜色、附件 ID 安全性、Controller 循环依赖。

---

## 七、执行结果（2026-07-24）

本报告提出的 P1 与可在现有接口下安全实施的 P2 已完成，处理结果如下。

### 已完成

1. 图片占位 SVG 的运行态颜色集中到 `lib/theme/runtime-colors.js`，优先读取 CSS token，保留仅用于 data URI 和无 DOM 环境的回退映射。
2. 标签默认色统一复用运行时主题映射；侧栏标签色值增加严格十六进制颜色归一化，拒绝通过颜色字段注入额外内联 CSS。
3. 附件 ID 从时间戳与 `Math.random()` 改为 `crypto.randomUUID()`。
4. `app-controller-registry.js` 使用现有 `controllerActions` 惰性动作端口处理跨 controller 调用，移除编辑器与导航间的可变双向闭包。
5. `local-attachment-store.js` 从 346 行拆为 CRUD 入口、跨平台文件路径管理和快照导入导出三个单一职责模块，所有业务源文件均低于 250 行。
6. `note-command-controller.js` 新增统一 mutation runner，集中 API 刷新链路与 local 同步链路。
7. 新增统一 `AppError`，接入 HTTP body 校验、内容标注与附件存储中需要携带 code/statusCode 的错误。
8. Note Repository 的 `deleteById` 统一为与 Folder/Tag Repository 一致的 `delete`。

### 经核对已由当前代码解决

- `milkdown-bundle.js`、CSS 与 source map 已在根 `.gitignore` 中排除，且不在 Git 跟踪列表；开发命令会在启动前自动构建，因此无需再次删除或调整 CI。

### 暂缓项

- `note-routes.js` 声明式路由表：涉及 15 条 HTTP 路由的匹配顺序，当前缺少逐路由入口级回归矩阵。应先补齐 method/path/params/status 测试，再单独迁移。
- 通用内存 Repository 工厂：Note Repository 包含排序、分页、软删除和批量删除等特有行为，现阶段强行抽象会增加间接层；本轮仅统一公共删除方法名。
- P3 中 app factory 继续拆分、React 探索目录去留、storage 业务模块化和全局错误码枚举仍属于架构路线决策，保留为后续专项。

### 验证

- `npm test`：API 118/118、Web 120/120 全部通过。
- 静态复核：目标文件无散落颜色字面量、附件 ID 无 `Math.random()`、注册层无可变 controller 环、带状态码错误无手工属性拼装。
- 本轮属于 `Unreleased` 批次内的兼容性重构与安全加固，不修改外部 API、数据格式和用户功能，因此不单独推进版本号。

---

*本报告由 AI 自动生成，基于代码静态分析与测试结果。*
*归档文件：`docs/已归档/代码体检报告-20260724.md`*

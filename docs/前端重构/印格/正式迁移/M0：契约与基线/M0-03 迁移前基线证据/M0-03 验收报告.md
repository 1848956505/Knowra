# M0-03 迁移前基线证据验收报告

## 验收信息

- 日期：2026-08-03
- 对应任务：M0-03 迁移前基线证据
- 审查对象：`M0-03 迁移前基线报告.md`、`截图/README.md` 和 21 张 JPEG 原件
- 主验收者：Codex
- 验收方式：Codex 独立完成，未调用 Claude。
- 执行差异：仅新增本验收报告并补录正式验收台账；未修改正式前端、API、迁移契约或基线原件。
- 结论：`PASS`

本结论只确认 M0-03 的迁移前证据交付物满足基线验收要求，不代表当前实现已经完成目标视觉迁移，也不代表 M0 阶段整体通过。

## 验收依据

- `docs/前端重构/正式迁移/02-分阶段实施任务书.md`：M0-03 要在正式 API/Web 和真实数据下采集首页、资料索引、编辑器、知识、训练的现状截图与控制台状态。
- `docs/前端重构/正式迁移/03-验收与回归方案.md:22-29`：每任务至少运行 `git diff --check` 与 `npm run test:web`。
- `docs/前端重构/正式迁移/03-验收与回归方案.md:45-53`：静态结构和运行证据不得以未解释错误、原型 hook 或不透明状态替代。
- `docs/前端重构/正式迁移/03-验收与回归方案.md:77-97`：截图需能说明视口、页面状态、面板状态和可复现条件；截图本身不能冒充完整可访问性结论。

## 交付物与矩阵核对

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 2048×1024 主矩阵 | PASS | 首页/当前默认资料索引、资料索引、编辑器四种侧栏组合、知识、训练，共 8 张 |
| 1440×1024 主矩阵 | PASS | 首页/当前默认资料索引、资料索引、编辑器、知识、训练，共 5 张 |
| 1280×720 主矩阵 | PASS | 首页/当前默认资料索引、资料索引、紧凑编辑器、知识、训练，共 5 张 |
| 1024×768 补充矩阵 | PASS | 默认入口、资料索引、编辑器覆盖式面板，共 3 张 |
| 截图文件清单 | PASS | 报告引用 21 张，目录实际 21 张；无缺失或未引用文件 |
| JPEG 文件有效性 | PASS | 所有原件均通过 `file` 检查；主矩阵和补充矩阵尺寸记录完整 |
| 差异标识 | PASS | `M0-03-baseline-2026-08-03` 已写入报告和运行说明 |

## 自动化与运行证据

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `npm run test:web` | PASS | `Web tests: 131 passed, 0 failed (of 131)` |
| `git diff --check` | PASS | 无输出、无空白错误；M0-03 目录额外空白扫描也无匹配 |
| 页面控制台 `warn/error` | PASS | 原报告记录 18 张主矩阵截图和 3 张补充截图均为空列表 |
| 外部 telemetry 噪声 | 已解释 | 2048×1024 训练采集期间的 Statsig 超时来自浏览器宿主外部请求，不属于 localhost 页面日志；原报告已单独记录 |
| 实时抽查 | PASS | 在临时端口实例上验证 2048×1024 页面 `screen=index`、知识/训练入口可达，页面日志为空，文档和页面宽度未出现额外横向溢出 |

实时抽查使用临时端口，是因为 3000/3001 当时被并行执行 agent 占用；端口占用属于执行环境事实，未作为 M0-03 缺陷。抽查结束后临时实例已关闭。

## 证据边界复核

- “首页”已明确标注为当前正式 Vanilla JS SPA 从 `/` 进入的默认资料索引，不虚构一个不存在的独立首页。
- 2048×1024 已覆盖编辑器双侧栏、仅目录栏、仅边注栏和无侧栏四种状态；1024×768 作为验收矩阵要求的最小视口补充证据保留。
- 知识工作域 1 条知识单元、训练工作域 0 道题目均被报告记录为真实数据状态，不能误读为空页面渲染失败。
- 报告明确说明当前截图是迁移前基线，不是原型与正式实现的视觉对比，也没有用截图宣称可访问性门禁通过。
- 2048×1024 和 1440×1024 的 JPEG 栅格受浏览器窗口捕获限制，分别记录为 1805×1024 和 1440×944；DOM 任务视口仍记录为 2048×1024、1440×1024。该限制已透明登记，后续比较应沿用相同采集管线和 DOM 视口。
- 真实工作区数据会随时间变化；后续迁移必须复用差异标识、页面状态和数据条件，不能只凭文件名比较。

## 缺陷与偏差

- P0：无。
- P1：无。未发现启动失败、核心页面缺失、未解释应用错误或任务视口/状态矩阵缺失。
- P2：无验收阻塞项。截图栅格限制、默认首页边界和真实数据变化属于已登记的基线约束，不是本任务的 UI 缺陷。
- 偏差记录：无新增 D-xxx；未修改 `05-决策与偏差记录.md`。
- 可访问性检查：本任务不改变实现；截图不能证明屏幕阅读器、键盘主路径或 200% 缩放通过，相关门禁留给后续实现任务。

## 结论与下一步

结论：`PASS`。

M0-03 已形成可追溯、可复用的迁移前基线证据，可作为 M1–M5 视觉比较和回归复核的输入。后续任务必须保持同一差异标识对应的数据和面板状态，并将截图与正式实现截图按同视口比较。

下一步：接受 M0-03 证据交付，继续后续迁移准入；M0-01 的 Q-004 仍按 A-001 的独立阻塞记录处理，不因 M0-03 PASS 自动解除。

# M2-03 纯功能导航浏览器验证证据

## 运行条件

- 日期：2026-08-06
- 页面：本地正式 API/Web 运行态，真实后端知识库数据。
- API：`http://localhost:3001`
- Web：`http://localhost:3000`
- 前置验收：M2-02 已由验收方记录为 `PASS`。
- 页面控制台：抽查状态下无 `error` / `warning`。

## 视口与截图

| 视口 | 页面/状态 | 实测结果 | 证据 |
| --- | --- | --- | --- |
| 1440×1024 | 资料索引，默认状态 | Grid 为 `208px 232px 1000px`；功能导航与资料库目录均可见；页面无横向溢出 | [资料索引截图](<浏览器证据/M2-03-1440x1024-资料索引.png>) |
| 1280×720 | 资料索引，紧凑高度 | Grid 为 `208px 232px 840px`；`scrollWidth=1280`，功能导航宽度仍为 208px | [紧凑资料索引截图](<浏览器证据/M2-03-1280x720-资料索引.png>) |
| 1024×768 | 资料索引，紧凑宽度 | Grid 为 `208px 232px 584px`；`scrollWidth=1024`，目录未侵入功能导航 | [1024 资料索引截图](<浏览器证据/M2-03-1024x768-资料索引.png>) |
| 1024×768 | 知识库工作域 | 目录隐藏，Grid 为 `208px 816px`；知识库入口激活；工作域主体可见；`scrollWidth=1024` | [1024 知识库截图](<浏览器证据/M2-03-1024x768-知识库.png>) |

## 交互核对

| 路径 | 结果 | 证据/说明 |
| --- | --- | --- |
| 资料库 → 知识库 | PASS | 主体切换为知识库，功能导航激活 `knowledge-overview`，资料库目录隐藏。 |
| 知识库 → 训练场 | PASS | 主体切换为训练场，功能导航激活 `training-overview`，资料库目录隐藏。 |
| 训练场 → 学习档案 | PASS | 主体切换为学习档案，功能导航激活 `learning-overview`，资料库目录隐藏。 |
| 学习档案 → 资料库 | PASS | 返回资料索引，功能导航激活 `materials`，资料库目录重新显示。 |
| 占位入口 | PASS | 15 个占位按钮同时具备原生 `disabled` 与 `aria-disabled="true"`，不产生业务切换。 |
| 设置入口 | PASS | 设置仍位于 TopBar；功能导航不存在设置项；点击设置前后页面仍为资料索引。 |

## 控制台与布局

- 1440×1024：`scrollWidth=1440`；三栏列宽为 208px / 232px / 1000px。
- 1280×720：`scrollWidth=1280`；三栏列宽为 208px / 232px / 840px。
- 1024×768 资料索引：`scrollWidth=1024`；三栏列宽为 208px / 232px / 584px。
- 1024×768 知识库：`scrollWidth=1024`；两栏列宽为 208px / 816px，目录隐藏。
- 页面控制台 error/warning 日志为空。

## 证据边界

本文件记录执行侧的运行态事实，不替代正式验收报告；浏览器截图均放在当前 M2-03 任务工作区的 `浏览器证据/` 子目录中。

# M4-05 资料边注执行报告

## 1. 执行结论

- 状态：执行完成，等待规划与验收方复验。
- 解锁依据：`06-验收记录.md` 的 A-030 已判定 M4-04 `PASS`，并明确解锁 M4-05。
- 任务范围：信息 / 大纲 / 重点 / AI Tab、标签与附件区域；未改动 Milkdown、API 契约、正式迁移计划和验收记录。

## 2. 实施内容

1. 为四个资料边注 Tab 补齐 `tablist` / `tab` / `tabpanel` 语义、`aria-selected`、roving `tabindex`，支持左右方向键、Home、End，并在重绘后恢复到激活 Tab 的焦点。
2. 保留既有信息、大纲、重点、AI renderer 与行为 selector，使用独立 `knowra-inkgrid-editor-marginalia.css` 完成焦点、标签、附件、折叠区和边注卡片的 InkGrid 样式收口。
3. 补齐附件重命名的正式事件链：输入草稿、Escape / 取消、表单提交；进入重命名后自动聚焦并选中文件名主体。
4. 保留大纲层级折叠和跳转、关联资料跳转、标注定位/删除、附件打开/右键/双击跳转等既有 controller 路径；未清理 M0-02 登记的历史 selector。

## 3. 自动化证据

- `node apps/web/test/events/aside-tag-events.test.js`：PASS。
- `node apps/web/test/m4-05-marginalia.test.js`：PASS。
- `npm run test:web`：PASS，144/144。
- `npm test`：PASS，API 210、Web 144、scripts 10。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS（仅工作区既有 LF/CRLF 提示，无空白错误）。

## 4. 运行态说明

- API `http://127.0.0.1:3000/api/health` 与 Web `http://127.0.0.1:3001` 均返回 HTTP 200。
- 本次会话的应用内浏览器被客户端策略阻止访问 `localhost` / `127.0.0.1`，因此没有伪造浏览器截图或宣称运行态视觉 PASS；视口视觉证据需由验收方在可访问本机服务的浏览器中复验。
- Tab、跳转、折叠、附件动作和焦点管理均已有 renderer、binder、controller 或专项自动化证据；正式 `PASS` 仍由验收方决定。

## 5. 影响文件

- `apps/web/lib/sidebar/renderers.js`
- `apps/web/lib/events/aside-events/{tabs,click,input,forms}.js`
- `apps/web/src/controllers/sidebar-controller.js`
- `apps/web/src/controllers/sidebar/attachment-rename-controller.js`
- `apps/web/styles/components/knowra-inkgrid-editor-marginalia.css`
- `apps/web/styles/components/knowra-inkgrid.css`
- `apps/web/test/events/aside-tag-events.test.js`
- `apps/web/test/m4-05-marginalia.test.js`

## 6. 验收建议

1. 在 1024 / 1280 / 1440 / 2048 视口检查右侧边注独立滚动及四个 Tab 的视觉状态。
2. 用键盘验证 Tab、Shift+Tab、左右方向键、Home、End 与焦点可见性。
3. 验证大纲跳转和父级折叠，确认不会破坏编辑器滚动恢复。
4. 使用含附件的资料验证打开、右键动作、双击引用跳转、重命名提交与 Escape 取消。


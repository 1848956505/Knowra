# M4-05 资料边注修复后复验验收报告

## 验收结论

- 日期：2026-08-11
- 对应任务：M4-05 资料边注
- 复验范围：A-031 的 M4-05-P1-1、M4-05-P1-2，以及 M4-05 四 Tab、键盘导航、边注折叠、大纲和附件动作回归。
- 执行差异：复验 `7e8f45f` 之上当前未提交工作区的 M4-05 修复实现、[M4-05 修复与复验报告](M4-05%20修复与复验报告.md)、自动化门禁和真实 API/Web 运行态。验收方未修改正式前端实现，仅新增本报告并更新正式验收台账。
- 结论：`PASS`

## 自动化与静态门禁

- `npm test`：PASS，API 210/210、Web 145/145、脚本 10/10。沙箱内首次运行仅因本机监听权限报 `listen EPERM`，在允许本地监听和子进程的环境复跑通过，无断言失败。
- M4-05 专项：`node apps/web/test/events/aside-tag-events.test.js`、`node apps/web/test/controllers/sidebar-controller.test.js`、`node apps/web/test/controllers/attachment-rename-controller.test.js`、`node apps/web/test/m4-05-marginalia.test.js` 均 PASS。
- Editor bundle：`npm run build:editor-bundle -w @study-accelerator/web` PASS。
- `git diff --check`：PASS。

## A-031 退回项关闭证据

### M4-05-P1-1 标签编辑重绘后的焦点连续性

- 信息 Tab 点击“添加标签”后，焦点进入 `data-note-tag-input`。
- 输入临时草稿 `验收临时` 触发边注重绘后，焦点仍在输入框，光标位置恢复到草稿末尾。
- 点击“收起”后，焦点返回 `data-note-tag-toggle` 的“添加标签”按钮。
- 在真实 `CLIP` 资料上添加并移除 `tag-space-demo-mastery-not-mastered`，两次重绘后焦点均保持在标签输入框；标签集合最终恢复为原有的“多模态、论文”。
- 结论：A-031 P1-1 已关闭。

### M4-05-P1-2 附件重命名后的焦点连续性

- 真实 `Transformer` 资料包含 7 个附件；通过附件资源条目右键打开的操作菜单包含“重命名”等 6 个动作。
- 进入重命名后，文件名主体输入框获得焦点并全选 `transformer-architecture-07`。
- Escape、取消按钮和成功提交三条路径均将焦点返回对应的 `data-attachment-open` 按钮，附件 `<details>` 保持展开。
- 成功提交曾将文件名临时改为 `transformer-architecture-07-acceptance-temp.png`，随后已提交恢复为 `transformer-architecture-07.png`。
- 结论：A-031 P1-2 已关闭。

## 边注语义与功能回归

- 四个 Tab “信息 / 大纲 / 重点 / AI”均可切换；`tablist`、`tab`、`tabpanel`、`aria-selected`、roving `tabindex` 与 `aria-labelledby` 关系一致。
- 从“信息”开始按 `ArrowRight`、`End`、`Home`、`ArrowLeft`，激活 Tab、焦点和面板关联均同步更新。
- 折叠边注后 `#kb-aside` 隐藏、重开按钮可见且焦点落到“展开资料边注”；重开后焦点回到“收起资料边注”。
- `Transformer` 大纲显示 69 个标题；父级折叠后对应子标题从 DOM 中移除，重新展开恢复；点击大纲项使编辑器滚动区滚动到目标位置。

## 多视口与运行态

| 视口 | 边注宽度 | 四 Tab | 边注独立滚动 | 页面横向溢出 | 结果 |
| --- | ---: | --- | --- | --- | --- |
| 1024×768 | 232px | 全部可见 | PASS | `1024 === 1024` | PASS |
| 1280×720 | 232px | 全部可见 | PASS | `1280 === 1280` | PASS |
| 1440×1024 | 232px | 全部可见 | PASS | `1440 === 1440` | PASS |
| 2048×1024 | 232px | 全部可见 | PASS | `2048 === 2048` | PASS |

- `.aside-panel-scroll` 在四档视口均有独立滚动高度；1024×768 实际滚动边注后，边注 `scrollTop` 由 557.5 增至 817.5，页面 `scrollTop` 保持 0。
- 真实 API/Web 运行态控制台 `error` / `warn` / `warning` 为空。
- 验收期间临时标签集合、附件名称和 CLIP 更新时间均已清理恢复；未留下正文、标签绑定或附件路径变更。

## 缺陷汇总

- P0：无。
- P1：无；A-031 P1-1、P1-2 已关闭。
- P2：无。
- 偏差记录：无新增，无已批准或未批准 D-xxx。
- 最终结论：`PASS`
- 下一步：解锁 M4-06；M4-07 继续执行完整编辑器状态矩阵与最终回归。

# M4-03 菜单栏与快捷工具修复后复验验收报告

## 验收结论

- 日期：2026-08-10
- 对应任务：M4-03 菜单栏与快捷工具
- 复验范围：A-028 的 P1-1、P2-1、P2-2，以及 M4-03 完整命令覆盖和多视口回归。
- 执行差异：复验 `8a54b83` 之上当前未提交工作区的 M4-03 修复实现、[M4-03 修复与复验报告](<M4-03 修复与复验报告.md>)、自动化门禁和真实 API/Web 运行态。验收方未修改正式前端实现，仅新增本复验验收报告并更新正式验收台账。
- 结论：`PASS`

## 自动化与静态门禁

- `npm test`：PASS（API 210/210、Web 142/142、脚本测试链路通过）。
- M4-03 专项：`editor-menu-renderers.test.js`、`events/menu-events.test.js`、`events/document-keyboard-events.test.js`、`editor-layout-state.test.js` 均 PASS；完整 Web 回归 PASS。
- Editor bundle：`npm run dev:all` 启动链路重建 Milkdown bundle PASS，API/Web 分别在 `3001` / `3000` 正常启动。
- `git diff --check`：PASS；A-028 P2-2 已关闭。
- 静态结构：新增 `menu-focus.js` 只负责定位菜单触发器或首个可用 `menuitem`，未新增平行命令控制器；文件、编辑、段落、格式、视图 action 属性保持不变。

## A-028 退回项关闭证据

### P1-1 “更多”打开/关闭与布局切换焦点链

- 1280×720 Overlay：激活“更多”后，弹层渲染 43 个正式 `menuitem`，`document.activeElement` 为首个可用菜单项“新建笔记”。
- Escape：聚焦“新建笔记”按 Escape 后，弹层关闭，焦点返回 `data-editor-menu-toggle="more"` 的“更多”按钮。
- Overlay → Full：“更多”弹层内存在焦点时将视口切至 2048×1024，布局进入 Full，窄模式弹层关闭，焦点返回可见“文件”触发器。
- Full → Compact：Full 下打开“段落”后，焦点为首个“正文” `menuitem`；将视口切至 1600×720 后进入 Compact，不可见的“段落”自动迁入“更多”，焦点进入其首个“新建笔记” `menuitem`。
- 焦点交接仅在重渲染前焦点位于编辑器菜单栏时执行，外部点击和命令执行关闭不强制抢占其他控件焦点。
- 结论：A-028 P1-1 已关闭。

### P2-1 关闭态 ARIA 控制目标

- 1024×768、1280×720、1440×1024、1600×720 的“更多”关闭态均不输出 `aria-controls`，DOM 中也不存在 `#editor-menu-more`。
- 打开后，触发器同步输出 `aria-controls="editor-menu-more"`，对应 `role="menu"` 目标同时存在；关闭后两者同步移除。
- Full 的文件/段落/编辑/格式/视图使用同一渲染规则。
- 结论：A-028 P2-1 已关闭。

### P2-2 工作区差异格式门禁

- 当前工作区 `git diff --check` 实测无输出，退回时的正式迁移 `README.md:44` 尾部空行告警已消失。
- 结论：A-028 P2-2 已关闭。

## 多视口与功能回归

| 视口 | 实际模式 | 可见菜单组 | 更多弹层 | 页面溢出 |
| --- | --- | --- | --- | --- |
| 1024×768 | Overlay | 文件、更多 | 43 项，320px，可滚动 | 无，`1024 === 1024` |
| 1280×720 | Overlay | 文件、更多 | 43 项，`x=262~582` | 无，`1280 === 1280` |
| 1440×1024 | Protected | 文件、视图、更多 | 43 项，`x=1104~1424` | 无，`1440 === 1440` |
| 1600×720 | Compact | 文件、编辑、视图、更多 | 43 项，`x=1028~1348` | 无，`1600 === 1600` |
| 2048×1024 | Full | 文件、段落、编辑、格式、视图 | Full 不显示更多 | 无，`2048 === 2048` |

- 菜单 Shell 在五档视口均为 `scrollWidth === clientWidth`，单行、无中文竖排、无无入口隐藏命令。
- Overlay / Protected / Compact 的“更多”均覆盖文件、编辑、段落、格式和视图五类共 43 个正式命令；Full 保留五组完整菜单和全部 8 个快捷工具。
- Protected 触发器与弹层未被边注层覆盖；所有弹层几何位于视口内。
- 运行态控制台 `error` / `warn` / `warning` 为空。

## 缺陷汇总

- P0：无。
- P1：无；A-028 P1-1 已关闭。
- P2：无；A-028 P2-1、P2-2 已关闭。
- 偏差记录：无新增，无已批准或未批准 D-xxx。
- 最终结论：`PASS`
- 下一步：解锁 M4-04 文档头与编辑纸面。

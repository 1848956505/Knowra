# M4-07 A-037 P1-1 二次修复后复验验收报告

## 验收结论

- 对应任务：M4-07 编辑器视觉与回归。
- 复验对象：A-037 未关闭的 200% 编辑器目录覆盖层、边注滚动区与长附件完整名称访问路径，以及 A-037 P2-1 验收数据恢复闭环。
- 执行差异：审查 `375f61e` 之上的当前未提交二次修复、[A-037 P1-1 二次修复与复验报告](<M4-07 A-037 P1-1 二次修复与复验报告.md>)、专项测试、全量门禁和真实 Chrome 200% 运行态；验收方未修改正式前端实现。
- 结论：`PASS`。
- 门禁：A-036、A-037 的 P1-1 均已关闭，A-037 P2-1 已关闭；M4-07 通过，M4 阶段完成，可解锁 M5-01。

## 自动化门禁

- `npm test`：PASS，API 210/210、Web 149/149、scripts 10/10。
- `node apps/web/test/events/library-index-events.test.js`：PASS。
- `node apps/web/test/editor-layout-state.test.js`：PASS。
- `node apps/web/test/m4-05-marginalia.test.js`：PASS。
- `node apps/web/test/m4-07-reflow.test.js`：PASS。
- `node apps/web/test/sidebar-renderers.test.js`：PASS。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS，94ms。
- `git diff --check`：PASS。

## 真实 Chrome 200% 复验

### 1. 首页与资料索引入口

- Chrome 实测 `outerWidth=1087`、`innerWidth=543`、`innerHeight=254`、`devicePixelRatio=4`。
- 首页和资料索引均保持 `document.documentElement.scrollWidth === innerWidth === 543`。
- 通过键盘 Enter 从首页进入资料库，在索引中收起目录和详情后打开活动长标题资料。

### 2. 编辑器目录覆盖层

- 从索引打开资料后，编辑器 `overlay` 状态可见原生“折叠目录栏”按钮。
- 对该按钮按 Enter 后，目录隐藏，焦点进入 `#library-index-directory-reopen`，可访问名称为“展开目录栏”。
- 目录关闭后 ShellBody 实测单列宽度 `543.5px`，编辑工作区宽度 `543.5px`，未再回退到 56px 隐式首列；页面宽度仍为 543px，无横向溢出。
- 对“展开目录栏”按 Enter 后目录恢复，焦点返回 `#library-index-directory-toggle`，可访问名称为“折叠目录栏”；关闭与重开焦点闭环通过。

### 3. 边注滚动区与长附件

- 200% 下 `.aside-panel-scroll` 实测 `clientHeight=53`、展开附件后 `scrollHeight=1429`、最大滚动距离 1376px，不再为 0 高度。
- 使用真实滚轮滚动至 `scrollTop=1375.75`，长附件进入当前视口，按钮 `isVisible=true`。
- 长文件名实测 `clientWidth=122`、`scrollWidth=534`、`overflow=hidden`、`text-overflow=ellipsis`，省略布局成立。
- 文件名 `<strong>` 和附件按钮的 `title` 均返回完整文件名；附件按钮 `aria-label` 完整包含文件名、`image/png` 与“已引用”状态。
- 长附件按钮可获得键盘焦点，当前焦点可访问名称完整，焦点态存在 3px 蓝色内侧标记。

### 4. 键盘回归与运行日志

- 正文完成临时输入后立即 `Command+Z`，撤销前后正文长度均为 31801，文本完全一致。
- “信息”Tab 按 `ArrowRight` 后，“大纲”成为当前焦点和选中 Tab。
- 应用来源未产生 `warn` / `error`；捕获消息均来自 `chrome-extension://` 浏览器扩展脚本，不计入应用错误。

## 验收数据恢复

- 复验前将基线数据及附件完整备份至 `/private/tmp/knowra-m407-a038.elSSpU`，基线 SHA-256 为 `09df4f6dc454596292d193c98f082aaf1578c566625f6bf3f5a57b5d5be44db7`。
- 使用本地 API 临时设置长目录、活动长标题和长附件名；浏览器复验结束后先停止 API/Web，再恢复 JSON 与整个附件目录。
- 恢复后 SHA-256 与基线一致；目录名、资料标题、附件名及附件路径均已回到 `多模态`、`Transformer`、`transformer-architecture-01.png` 和原路径。
- A-037 P2-1 不再复现，数据恢复闭环通过。

## 本轮证据

- [真实 Chrome 200% 编辑器目录关闭入口](<验收方第三次复验证据/01-Chrome-200-editor-directory-toggle.png>)
- [真实 Chrome 200% 目录关闭后主区宽度](<验收方第三次复验证据/02-Chrome-200-directory-closed-main-width.png>)
- [真实 Chrome 200% 长附件可滚动到达](<验收方第三次复验证据/03-Chrome-200-long-attachment-reachable.png>)
- [真实 Chrome 200% 长附件键盘焦点](<验收方第三次复验证据/04-Chrome-200-long-attachment-focus.png>)
- [真实 Chrome 200% 大纲 Tab 键盘焦点](<验收方第三次复验证据/05-Chrome-200-outline-keyboard-focus.png>)

## 判定与下一步

- P0：无。
- P1：无；A-036、A-037 P1-1 已关闭。
- P2：无；A-037 P2-1 已关闭。
- 偏差记录：无新增，无已批准或未批准 D-xxx。
- 最终结论：`PASS`。
- 证据边界：本轮确认任务约定的 200% 重排、键盘主路径和长内容状态，不扩展声明为全站 WCAG 合规认证。
- 下一步：结束 M4 阶段，按任务书解锁 M5-01 知识工作域适配。

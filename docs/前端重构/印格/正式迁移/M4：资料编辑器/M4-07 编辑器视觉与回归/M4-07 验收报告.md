# M4-07 编辑器视觉与回归验收报告

## 验收结论

- 对应任务：M4-07 编辑器视觉与回归
- 验收范围：M4 完整编辑器状态矩阵、功能回归、可访问性门禁、自动化门禁与真实 API/Web 运行态。
- 执行差异：审查提交 `ab55b66` 之上的当前未提交 M4-07 实现、[M4-07 执行报告](<M4-07 执行报告.md>)、浏览器证据、专项测试及真实运行态；验收方未修改正式前端实现。
- 结论：`REJECTED`
- 门禁：已提供状态与修复本身通过，但正式验收矩阵证据不完整，暂不结束 M4 阶段、不解锁后续阶段。

## 自动化门禁

- `node apps/web/test/controllers/tab-overflow-controller.test.js`：PASS。
- `node apps/web/test/events/document-keyboard-events.test.js`：PASS。
- `npm run test:web`：PASS，148/148。
- `npm run test:api`：PASS，210/210。
- `npm run test:scripts`：PASS，10/10。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS，仅存在 Git 行尾转换提示，无空白错误。

## 已通过的真实运行态检查

- 1280×720 下，主页、资料索引、首次打开非空资料、源码分栏、阅读态专注、十标签溢出均可运行，页面 `scrollWidth === innerWidth`。
- 非空资料“测试”首次打开时正文立即显示，Milkdown Host 唯一。
- 源码分栏可打开与关闭；阅读态进入专注后 `[contenteditable="true"]=0`，正文保持可见，焦点位于“退出专注模式”。
- 十个打开标签形成 8 个可见标签和 2 个隐藏标签；隐藏标签入口可见，活动标签保持可达。
- 溢出菜单按 Escape 关闭后，`aria-expanded="false"`，焦点返回“显示其余 2 个标签页”按钮；本轮焦点修复通过。
- 浏览器运行日志为空，未发现 `error`、`warn` 或 `warning`。
- 提交的 2048×1024、1440×1024、1280×720、1024×768 截图中，已提供的四种侧栏组合、源码分栏、阅读态专注及标签溢出未见页面级横向溢出或菜单竖排。

## 未通过项

### M4-07-P1-1 正式编辑器状态与可访问性矩阵未完整验收

`03-验收与回归方案.md` 要求编辑器状态覆盖长面包屑、长中文标题、附件长文件名，并要求 200% 缩放后主路径可用。当前交付缺少以下可复核证据：

- 没有 200% 浏览器缩放后的首页 → 资料索引 → 打开资料 → 编辑 → 切换边注 → 返回主路径记录，也没有对应 DOM、截图或辅助技术检查。验收浏览器发送缩放快捷键后页面有效视口未变化，因此不能把本轮尝试记为 200% 验证通过。
- 执行报告明确说明当前数据没有长文件名附件，并以 renderer、事件、controller 测试代替真实状态；这不能证明长附件文件名在正式布局中的视觉溢出处理。
- `08-2048x1024-十标签与长标题.png` 的活动资料为短标题“测试2”；长中文标题只作为非活动标签被截断展示，没有覆盖长标题作为当前文档标题及长面包屑时的主编辑区布局。

该项不是已登记且获批准的偏差。根据验收方案，“证据不足”必须判定 `REJECTED`，且 M4-07 的任务验收要求是“M4 验收矩阵全部通过”。

## 判定与下一步

- P0：无。
- P1：1 项；M4-07-P1-1 正式状态与可访问性矩阵证据不完整。
- P2：无。
- 偏差记录：无新增，无已批准或未批准 D-xxx。
- 最终结论：`REJECTED`。
- 必须补充：以非生产或可恢复的验收数据完成长面包屑、活动长中文标题、附件长文件名的真实布局检查；在真实 200% 浏览器缩放下完成键盘主路径、焦点可见性与页面级溢出检查，并记录视口、数据状态、面板状态和差异标识。
- 下一步：补齐上述矩阵和证据后复验；通过前不结束 M4、不进入后续阶段验收。

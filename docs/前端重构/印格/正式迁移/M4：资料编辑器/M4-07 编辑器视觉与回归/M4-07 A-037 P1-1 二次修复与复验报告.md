# M4-07 A-037 P1-1 二次修复与复验报告

## 1. 执行结论

- 输入依据：[M4-07 A-036 P1-1 修复后复验验收报告](<M4-07 A-036 P1-1 修复后复验验收报告.md>) 与 `06-验收记录.md` A-037。
- A-037 指出的编辑器目录覆盖层、边注滚动区高度为 0、长附件完整名称访问三个阻断均已修复，并在真实 Chrome 200% 下完成运行态复测。
- 本报告只记录执行方修复与自验；不修改 A-037 `REJECTED` 结论，不代替验收方关闭 P1 或结束 M4。

## 2. 修复内容

### 2.1 编辑器目录覆盖层关闭与重开

- 复用目录头已有按钮，在编辑器 `overlay` 布局中显示“折叠目录栏”；关闭后显示 ShellBody 级“展开目录栏”按钮。
- 索引页继续修改 `state.libraryIndex.directoryOpen`；编辑器按钮独立修改 `state.view.showLeftSidebar`，避免两种偏好互相覆盖。
- 键盘关闭后焦点交给“展开目录栏”，键盘重开后焦点返回“折叠目录栏”。
- 补充双隐藏重排规则：功能导航与目录都关闭时 ShellBody 只保留 `minmax(0, 1fr)` 主列，避免 EditorMain 落入 56px 隐式首列。

### 2.2 200% 边注滚动区

- 200%/700px 以下将边注覆盖层改为 `auto auto minmax(0, 1fr)` 三行 Grid，移除桌面态底部大 padding 与外边距。
- 标题与 Tab 收敛为 32px；隐藏装饰性 `MARGINALIA`，阻止窄宽换行把标题轨道撑至 70px。
- `.aside-panel-scroll` 保持 `overflow-y: auto` 和 `overscroll-behavior: contain`，真实 Chrome 中从 `clientHeight=0` 修复为 `53px`，内容 `scrollHeight=682px`；展开附件后 `scrollHeight=1429px`、最大滚动距离 `1376px`。

### 2.3 长附件完整名称

- 省略文本 `<strong>` 直接提供完整 `title`。
- 可聚焦附件按钮保留完整 `title`，并增加包含文件名、MIME 与引用状态的 `aria-label`。
- 真实长附件名仍保持 `clientWidth=122`、`scrollWidth=534`、`overflow=hidden`、`text-overflow=ellipsis`，且已通过滚动进入 53px 可视滚动区。

## 3. 真实 Chrome 200% 复测

- 浏览器：`outerWidth=1087`、`innerWidth=543`、`innerHeight=254`、`devicePixelRatio=4`。
- 页面宽度：编辑器 `document.documentElement.scrollWidth === innerWidth === 543`。
- 目录打开：编辑器 `overlay` 布局中“折叠目录栏”可见。
- Enter 关闭目录：`sidebar.hidden=true`，焦点为 `#library-index-directory-reopen`；ShellBody 主列为 `543.5px`，EditorMain 为 `544px`。
- Enter 重开目录：目录恢复，焦点为 `#library-index-directory-toggle`。
- 边注：面板 125px，标题 32px，Tab 32px，滚动区 53px。
- 长附件：通过真实滚轮滚动到可见范围；截断节点 `title` 与按钮 `title`/`aria-label` 均返回完整文件名。
- 应用自身无 `warn` / `error`；捕获到的消息均来自浏览器翻译扩展的 `chrome-extension://` 脚本。

## 4. 自动化门禁

- `npm test`：PASS，API 210/210、Web 149/149、scripts 10/10。
- `node apps/web/test/events/library-index-events.test.js`：PASS。
- `node apps/web/test/editor-layout-state.test.js`：PASS。
- `node apps/web/test/m4-05-marginalia.test.js`：PASS。
- `node apps/web/test/m4-07-reflow.test.js`：PASS。
- `node apps/web/test/sidebar-renderers.test.js`：PASS。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS。

## 5. 数据恢复

- 复测前备份 `storage/data/knowledge-base.json` 与 `storage/uploads/` 至 `/tmp/knowra-m4-07-a037.s1zRLp`。
- 临时设置长目录、活动长标题和长附件名；复测结束后先停止 API/Web，再恢复 JSON 与整个附件目录。
- 恢复后 `knowledge-base.json` SHA-256 为 `09df4f6dc454596292d193c98f082aaf1578c566625f6bf3f5a57b5d5be44db7`，`git status --short storage` 无输出。

## 6. 证据

- [真实 Chrome 200% 编辑器目录关闭入口](<再次修复后复验证据/01-Chrome-200-editor-directory-toggle.png>)
- [真实 Chrome 200% 长附件可滚动到达](<再次修复后复验证据/02-Chrome-200-long-attachment-reachable.png>)

## 7. 待验收方复核

1. 从索引打开资料后，使用键盘关闭和重开编辑器目录覆盖层，确认按钮可见且焦点交接正确。
2. 关闭目录后确认正文获得完整主区宽度，没有 56px 隐式列回退。
3. 在 200% 下展开边注附件并滚动到长文件名，确认省略、完整 `title` 和可访问名称。
4. 正式 `PASS`、A-036/A-037 关闭与 M4 阶段结束仍由验收方写入验收记录。

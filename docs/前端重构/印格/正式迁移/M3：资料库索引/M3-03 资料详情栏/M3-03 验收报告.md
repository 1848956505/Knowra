# M3-03「资料详情栏」验收报告

## 1. 当前验收结论

`PASS`

- 验收日期：2026-08-08
- 验收方：Codex
- 对应任务：M3-03「资料详情栏」
- 验收基线：当前 `codex/frontend-refactor` 工作区、[M3-03 执行报告](<M3-03 执行报告.md>)、A-019 M3-02 通过记录及本次真实 API/Web 运行态。
- 验收依据：[迁移契约与功能对等清单](<../../01-迁移契约与功能对等清单.md>)、[验收与回归方案](<../../03-验收与回归方案.md>)、M3-03 任务说明。

详情栏已按 232px 目标迁移完成。真实资料字段、选择同步、内容滚动、折叠/重开、资料打开返回和紧凑视口均通过；M3-03 任务要求的验收门禁满足，本次允许解锁后续任务。

## 2. 验收范围

- 资料索引右侧 232px 详情栏的宿主、布局和真实信息展示。
- 标题、类型、路径、字数、更新时间、收藏、标签、关联资料、大纲和附件信息。
- 索引行选择后详情同步；详情栏关闭、重开及当前资料上下文保持。
- 大纲等长内容的独立滚动、详情栏打开资料及返回索引。
- 1024×768、1280×720、1440×1024、2048×1024 的布局、页面级横向溢出和折叠按钮可达性。
- 关闭/重开按钮的原生语义、`aria-expanded`、`aria-controls` 和详情分组的原生 `details/summary` 语义。

## 3. 自动化与静态检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm test` | PASS | API 210/210、Web 140/140、脚本 10/10，退出码 0 |
| `npm run test:api` | PASS | API 210/210 |
| `npm run test:web` | PASS | Web 140/140 |
| `npm run test:scripts` | PASS | 脚本 10/10 |
| `git diff --check` | PASS | 无空白错误 |
| 详情栏相关 Web 回归 | PASS | 渲染器、事件委托、SSR 宿主、样式 token 和紧凑布局断言均随 Web 测试通过 |

## 4. 浏览器运行态复验

### 4.1 真实字段与资料信息

- 全部资料范围下选中真实资料 `Transformer`，详情栏显示：类型“手动笔记”、路径“资料 / 多模态”、字数 `11595`、最后编辑 `2026.07.31 21:47`、收藏“未收藏”、标签“暂无标签”、关联笔记 `0`、内容大纲 `78`、附件 `0`。
- 切换到 `本项目` 后选中 `二、右侧知识点面板的整体结构`，详情栏显示真实路径“资料 / 本项目”、字数 `2470`、最后编辑 `2026.07.18 22:25`，内容大纲 `35`；没有附件时显示“打开资料后可管理附件”，未伪造附件项。
- 详情栏保留“打开”入口；点击后进入该资料编辑器，标题为“二、右侧知识点面板的整体结构”，点击返回后回到资料索引并保留详情上下文。

### 4.2 视口矩阵

| 视口 | 详情栏计算宽度 | 运行态布局 | `document/body.scrollWidth` | 折叠按钮 |
| --- | ---: | --- | ---: | --- |
| 2048×1024 | 232px | 并列，索引区 + 详情栏 | 2048 / 2048 | 可达 |
| 1440×1024 | 232px | 并列，索引区 + 详情栏 | 1440 / 1440 | 可达 |
| 1280×720 | 232px | 并列，索引区 + 详情栏 | 1280 / 1280 | 可达 |
| 1024×768 | 232px | 右侧覆盖层 | 1024 / 1024 | 可达 |

四档视口均无页面级横向溢出。1024px 紧凑布局的详情栏仍为 232px，覆盖层右侧定位不改变内容契约。

### 4.3 选择同步、滚动与折叠

- 选择 `本项目` 下第二条资料后，详情标题由“二、右侧知识点面板的整体结构”同步为“知识点”，详情路径仍为“资料 / 本项目”，列表项的 `data-selected` 同步为当前资料。
- 重新选择长大纲资料并展开“内容大纲”，详情独立滚动容器的 `scrollTop` 从 `0` 变为 `580`；页面本身 `scrollX=0`、`scrollY=0`，滚动未把详情栏关闭按钮推出可视区域。
- 1280×720 下点击“收起详情”后详情栏进入 `data-open="false"`、计算宽度 `0px`，重开按钮可见且带 `aria-expanded="false"`；点击“展开详情”后恢复 `data-open="true"`、232px 宽度、当前资料标题和上下文。
- 1024×768 覆盖层同样完成关闭/重开：关闭态详情栏宽度为 `0px`、重开按钮可见，页面宽度仍为 1024；重开后恢复 232px，关闭按钮可见且 `aria-expanded="true"`。

### 4.4 可访问性与运行日志

- 详情栏宿主保留 `aria-label="资料详情"`；展开态“收起详情”和折叠态“展开详情”均为原生 `button`，`aria-controls="library-index-inspector"` 指向存在的宿主，`aria-expanded` 随状态同步。
- 关联资料、附件和资料打开入口均使用可聚焦按钮；大纲、关联资料、附件分组使用原生 `details/summary`，不依赖可点击 `div`。
- 运行态控制台 `error` / `warn` / `warning` 均为空。
- 640px CSS 窄视口补测显示当前应用外壳仍保留 1024px 最小宽度；这是全局外壳的既有响应式边界，未纳入本次 M3-03 的四档验收基线，完整 200% 全站适配需另行追踪，未将其作为本任务详情栏缺陷登记。

## 5. 缺陷、偏差与验收边界

- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 验收边界：验收方未修改正式前端实现；本次仅新增本验收报告并更新正式验收记录。附件动作在无附件的真实数据下未执行实际打开，但附件信息展示、按钮渲染和事件委托已由代码检查及 Web 回归覆盖。

## 6. 最终结论与下一步

M3-03 资料详情栏验收结论为 `PASS`。232px 详情栏、真实字段、选择同步、独立滚动、折叠/重开、打开返回及紧凑视口均通过，允许进入后续迁移任务。

# V4-04 复验报告：Token 与基础组件（修复轮）

- 复验日期：2026-08-19 晚
- 触发：[V4-04-验收报告-20260819.md](./V4-04-验收报告-20260819.md) 给出 2 个 P0 + 6 个 P1
- 范围：仅 V4-04 关联文件，未触碰 V4-00.5 历史候选/游戏化主题/备份/部署/其他无关目录
- 结论：**P0/P1 全部修复；自动化与真浏览器复验通过；等待验收 Agent 二次复核后决定是否发放 V4-05 准出。**

## 1. 修复对照

| 验收项 | 修复点 | 证据 |
| --- | --- | --- |
| P0-1 Dialog 取消不关闭 | 抽出 `DialogClose` 组件，通过 React Context 暴露 `close()`；业务按钮形态继承 `Button` 视觉，可直接写 `<DialogClose variant="ghost">取消</DialogClose>`；图标按钮形态默认 `aria-label="关闭对话框"` | `src/components/ui/overlay/Dialog.tsx`；Playwright `Dialog 关闭契约` 4 个测试通过；RTL `Dialog.test.tsx` 仍通过 |
| P0-2 390px GridList 仍压缩 | `Collection.module.css` 新增 ≤767 行布局：grid-template-areas "title more" "meta  meta"；overflow: hidden 禁止组件内部横滚；GridListColumn 标记 `title: true` 让 cell 走两行省略；测试用 `grid.scrollWidth ≤ grid.clientWidth + 1` 断言 | `Collection.module.css` `@media (max-width: 767px)` 段；Playwright `移动端 GridList（两行紧凑列表，不横滚）` 通过 |
| P1-1 空 SearchField 仍显示清除 | 内部 `useState` + 受控/非受控双形态；按 `currentValue.length > 0` 条件渲染 `search-clear` 按钮；点击后 `requestAnimationFrame` 把焦点回到 input | `SearchField.tsx`；Playwright `SearchField 清除按钮契约` 通过；RTL 调整后通过 |
| P1-2 展台无条件请求 workspace API | `loadWorkspace` 从 `App` 顶层 effect 下沉到 `WorkspaceOverview` 组件；`/#/showcase` 路由不再触发 `WorkspaceApi` 调用 | `App.tsx`；Playwright `独立展台运行不调用 workspace API` 通过（监听 console + network，断言 0 warning/error、0 `/api/knowledge/*` 请求） |
| P1-3 无视觉快照 | 新建 `e2e/visual-baseline/component-showcase.spec.ts`：桌面 1440×900 5 张（顶区/输入/Dialog/Menu/集合）、移动 390×843 2 张（顶区/GridList）、200% 缩放 1 张；保留 `e2e/component-showcase.spec.ts` 做结构断言 | `apps/web-v4/e2e/visual-baseline/screenshots/*.png` 8 张 |
| P1-4 390px 展台头部拥挤 | `.app-header` 在 `global.css` 767 折断：flex-direction column、44px 触控；`.hero` 同步改 column；标题与副标题补 word-break/overflow-wrap | `global.css` `.app-header` 段；`Showcase.module.css` `.hero` 段；Playwright `移动端顶区 + Hero` 通过 |
| P1-5 硬编码阴影 / rgba | 新增 Token `--shadow-card` / `--shadow-card-danger` / `--shadow-dialog` / `--shadow-badge` / `--ink-rgb` / `--ink-bg-rgb` / `--scrim-underlay` / `--scrim-busy`；CSS Module 全部替换；`tokens.css` 顶部登记为 V4-04 扩展 | `tokens.css` 注释；grep `box-shadow\|rgba\(` 结果只剩 token 引用 |
| P1-6 GridList 伪表头 | `GridListColumn` 去掉 `header` 字段；`GridList` 不再渲染 `RAGridListHeader`；按验收 P1-6 选"选择列表，去掉伪表头"路径 | `GridList.tsx` `GridList.test.tsx` |

## 2. 自动化门禁

| 项 | 结果 |
| --- | --- |
| `npm run check`（typecheck + boundary + vitest） | PASS |
| `npm test` | 13 files / **56 tests passed** / 0 failed |
| `npm run build` | PASS；27.42 kB CSS / 511.02 kB JS（gzip 5.45 kB / 157.01 kB）；保留 500 kB chunk 警告（V4-05 再做 code-split） |
| `npm run test:e2e` | **21 / 21 passed** |
| `git diff --check` | exit 0（autocrlf 警告为 Windows CRLF 行为，不影响 diff 校验） |
| V3 / React Aria 边界 | PASS（`check-v4-boundaries.mjs`） |

## 3. 真实 Chromium 复验覆盖

- 桌面 1440×900：顶区 + 按钮、输入区、Dialog 打开（双层硬阴影）、Menu 展开、Tree/GridList/TagGroup 集合组件。
- 移动 390×843：顶区 + Hero（不挤压不触字断行）、GridList 两行紧凑列表 + 内部不横滚断言。
- 200% 缩放 1280×720：document.zoom=2 后 documentElement / body scrollWidth - clientWidth ≤ 2。
- Dialog 4 种关闭路径：取消按钮、确认按钮、右上角 X、Escape，每条都验证 focus 归还 trigger。
- SearchField 3 态：空值无按钮 → 输入后出现 → 点击清除并保留焦点。
- 独立展台 console 0 warning/error、0 `/api/knowledge/*` 请求。

## 4. 截图证据

- `apps/web-v4/e2e/visual-baseline/screenshots/desktop-hero-buttons.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/desktop-input-section.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/desktop-dialog-open.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/desktop-menu-open.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/desktop-collection-section.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/mobile-hero.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/mobile-gridlist.png`
- `apps/web-v4/e2e/visual-baseline/screenshots/zoom-200-percent.png`

## 5. 剩余边界

- 验收报告 §7 提到的 NVDA / VoiceOver 实测未做；V4-04 仍按 RAC 语义 + 自动化断言保证 WCAG 主路径。
- `git diff --check` 在 Windows 上输出 CRLF 警告；属于 autocrlf 行为，已确认 exit code 0。
- P3 项（Remix Icon 本地化、Milkdown toolbar 适配）按 V4-04 任务书 §8 不在本轮范围。
- 510.39 kB chunk 警告留到 V4-05 通过 dynamic import 解决；本轮不阻塞 V4-04 准出。

## 6. 下一动作

- 不领取 V4-05，等验收 Agent 在真浏览器对 1440×1000 / 390×843 / 1280×720@200% / console 截图复核后，再标记 V4-04 为"已冻结"。
- 一旦验收通过，工程变更日志 [Unreleased] 已写好的 V4-04 条目保持不变。

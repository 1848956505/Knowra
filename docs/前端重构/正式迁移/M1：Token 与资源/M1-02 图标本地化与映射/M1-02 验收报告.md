# M1-02 图标本地化与映射验收报告

## 1. 验收结论

`REJECTED`（暂不通过）。

自动化门禁和本地资源服务验证均通过，但正式运行时仍有 3 处图标直接使用 Unicode `›`，未接入 M1-02 的语义图标映射，且未登记为批准偏差。按照《03-验收与回归方案》，“未登记偏差”不能判定为 `CONDITIONAL PASS`。

- 日期：2026-08-04
- 对应任务：M1-02 图标本地化与映射
- 验收版本：`faef0fd`（`codex/frontend-refactor`）
- 验收方：验收复核

## 2. 验收范围

- 本地 Remix SVG 资源与中央语义映射。
- Shell、目录树、资料索引、搜索、侧栏、编辑器及 Milkdown 图标接入。
- 运行时无 CDN、无页面内复制 SVG path。
- 装饰图标的 `aria-hidden` 与交互按钮的中文可访问名称。
- 全量回归、Editor bundle 构建及静态资源离线可用性。

## 3. 通过项

- `apps/web/lib/icons/icon-map.js` 提供 64 个语义名称，映射至 53 个本地 Remix SVG 资源。
- `node apps/web/test/icon-mapping.test.js`：PASS。
- `npm run test`：API 210/210、Web 133/133、脚本 10/10 PASS。
- `npm --prefix apps/web run build:editor-bundle`：PASS。
- `git diff --check`：PASS。
- 独立静态资源验证：53/53 个映射资源均返回 `200 image/svg+xml`。
- 正式渲染器未发现旧 Phosphor 路径、CDN URL 或内联 UI SVG path。
- 已检查的交互按钮保留中文 `aria-label`/`title`；本次发现未造成键盘或可访问名称回归。

## 4. 未通过项

### P2-1：残留 Unicode 图标，未接入中央映射

以下正式运行时位置仍直接输出 `›`：

1. `apps/web/src/server/shell-html.js:87`：资料边注收起按钮。
2. `apps/web/lib/library-index/renderers.js:63`：空状态索引检查器收起按钮。
3. `apps/web/lib/library-index/renderers.js:80`：已选资料检查器收起按钮。

M1-02 的任务书要求确定 Remix 图标清单并建立语义名到资源路径的映射，验收要求断网显示图标且图标语义正确。上述位置属于 Shell/资料索引交互图标，但未调用 `renderIcon()`。

现有 `icon-mapping.test.js` 只检查旧 Phosphor、CDN、内联 SVG 和直接资源路径，未检查目标渲染器中的 Unicode 图标字符，因此测试通过不能覆盖该遗漏。

## 5. P0/P1 与偏差

- P0：无。
- P1：无。按钮功能和可访问名称未发现回归。
- P2：1 项，包含 3 处残留图标。
- 偏差记录：未发现已登记、已批准的偏差。

## 6. 复验要求

1. 为收起/折叠操作补充语义图标映射，或复用现有 `navigationChevron`，替换上述 3 处 `›`。
2. 增加回归检查，禁止正式图标渲染器直接输出该类 Unicode 图标字符。
3. 重跑 `node apps/web/test/icon-mapping.test.js`、`npm run test`、Editor bundle 构建和 `git diff --check`。
4. 补充新的验收记录后，重新判定 `PASS` 或 `CONDITIONAL PASS`。

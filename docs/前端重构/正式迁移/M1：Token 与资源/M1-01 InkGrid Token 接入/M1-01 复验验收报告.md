# M1-01 InkGrid Token 接入复验验收报告

## 验收信息

- 日期：2026-08-04
- 对应任务：M1-01 InkGrid Token 接入
- 验收方：Codex
- 验收对象：修复与复验报告、更新后的执行报告、Token 验证证据、Token 文件及关联 CSS/测试改动。
- 历史结论：A-004 为 `REJECTED`；本报告为修复后的独立复验，不覆盖历史记录。

## 自动化门禁

- `node apps/web/test/inkgrid-token-shape.test.js`：PASS。
- `git diff --check`：PASS。
- `npm run test:web`：PASS，132 passed / 0 failed。
- `npm run test`：PASS，API 210、Web 132、脚本 10/10；脚本测试使用本机 Git Bash PATH 前提。

## 验收检查

- `--ink-*` canonical Token 覆盖字体、色彩、线条、阴影、尺寸、间距、字号和圆角。
- 旧变量兼容别名仍解析到 canonical Token；值域测试覆盖字体角色、无衬线约束和关键字号。
- 设计基线已对齐：无衬线字体栈、Display 42px、页面 H1 28px、区块 H2 16px。
- 5 个生产 CSS Token 消费点已在执行报告中完整披露，并说明为 M0-01 冻结激活线规则的必要消费点。
- 当前修复 diff 未涉及页面结构、控制器、状态语义、API 或事件 selector；未发现新增组件 CSS 硬编码视觉值。
- 1280×720 首页和编辑器运行态证据可加载；修复后的 computed style 记录与 Token 设计基线一致，未见页面级横向溢出。
- 本任务未改变交互语义，键盘、屏幕阅读器和 200% 缩放门禁由后续 UI 迁移任务独立验收。

## 缺陷与偏差

- P0：无。
- P1：无。A-004 的未披露生产 CSS 范围和排印基线偏差均已收口。
- P2：无。
- 偏差记录：无已批准 D-xxx。

## 结论

`PASS`

允许解锁 M1-02、M1-03。页面结构宽度和完整视觉迁移仍须按后续任务单独验收。

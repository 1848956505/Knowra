# M1-01 InkGrid Token 接入修复与复验报告

## 修复范围

原《M1-01 验收报告》结论为 `REJECTED`，提出两项 P1：实际修改的 5 个 Token 消费 CSS 未在执行报告披露，以及 canonical 排印 Token 与当前 InkGrid 设计基线不一致。

本次修复：

- 在执行报告中补齐 5 个实际生产 CSS 文件，并说明它们只是 M0-01 已冻结激活线规则的必要 Token 消费点；未新增页面结构、控制器、API、状态语义或事件 selector 改动。
- 将 UI、阅读、展示字体统一到设计文档规定的无衬线栈，等宽字体切换为 `"SF Mono", "Menlo", monospace`；兼容字体别名仍解析到 canonical Token。
- 将关键排印基线修正为 Display `42px`、页面 H1 `28px`、区块 H2 `16px`，并同步调整 H3/H4 值域。
- 为字体角色、无衬线约束、关键字号和兼容别名增加自动化断言。

## 复验结果

修复后的 Token 形状测试、Web 测试和完整 `npm test` 已通过：API 210、Web 132、脚本 10；浏览器 1280×720 computed style 也已复核，`git diff --check` 无错误。

原验收报告保留不覆盖，作为修复前的审查记录；当前任务状态为“待验收方复验”。

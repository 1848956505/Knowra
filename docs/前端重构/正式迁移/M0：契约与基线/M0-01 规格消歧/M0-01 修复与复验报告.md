# M0-01 修复与复验报告

## 任务信息

- 对应任务：M0-01 规格消歧
- 复验日期：2026-08-03
- 工作区：`docs/前端重构/正式迁移/M0：契约与基线/M0-01 规格消歧/`
- 复验性质：针对原验收报告退回项的执行补充，不替代规划与验收方的正式结论。

## 当前结论

`Tab 实现已修复 / Q-004 目标原型证据已补齐 / Q-001～Q-003 等待正式决策落库`

原 [M0-01 验收报告](<M0-01 验收报告.md>) 保留为历史验收记录，不能直接改写为 `PASS`。本报告记录本轮实际修复和可交给规划与验收方复审的证据。

## 本轮修复

### 正式前端 Tab

- 文档标签页：激活态使用 `2px` 左侧蓝线，白底，黑色文字。
- 模块、资料视图、边注面板 Tab：激活态使用 `2px` 下方黑线和黑色文字。
- 新增 `--border-tab-active: 2px`，避免复用 3px 的通用强调线。
- 未修改 API、状态语义、事件 selector、数据结构或编辑器行为。

涉及文件：

- `apps/web/styles/components/knowra-theme-tokens.css`
- `apps/web/styles/components/note-tabs.css`
- `apps/web/styles/components/aside-tabs.css`
- `apps/web/styles/components/knowra-editor.css`
- `apps/web/styles/components/knowra-library-index.css`
- `apps/web/styles/components/work-domain.css`
- `apps/web/test/knowra-redesign-ui.test.js`

### 规格来源同步

已同步修订设计文档和组件库中的旧冲突：

- 功能导航 `208px`。
- 目录栏、`IndexInspector`、`MarginaliaPanel` `232px`。
- `InspectorWide` 显式宽版 `300px`。
- 目录示例不再把 `220px` 当作生产目录宽度；`220px` 只保留给菜单。
- 文档 Tab 改为左侧蓝线；模块/视图/面板 Tab 统一为下方黑线。

## Q-004 浏览器证据

使用本地只读 HTTP 服务 `http://localhost:8765/`，在 CSS 视口 `1440 × 1024` 下重新打开三张目标原型。页面控制台 `warn/error` 均为空，页面 `scrollWidth` 与 `innerWidth` 均为 `1440`。

| 目标页面 | 页面标题 | 截图 | 页面控制台 |
| --- | --- | --- | --- |
| 主页 | 印格 · 知境 Knowra 主页 | [M0-01-home-1440.png](<浏览器证据/M0-01-home-1440.png>) | 空 |
| 资料库索引页 | 印格 · 资料库索引页 | [M0-01-index-1440.png](<浏览器证据/M0-01-index-1440.png>) | 空 |
| 资料编辑器页 | 印格 · 资料编辑器页 | [M0-01-editor-1440.png](<浏览器证据/M0-01-editor-1440.png>) | 空 |

浏览器截图接口输出的 JPEG 实际栅格为 `1440 × 944`，DOM 记录的 CSS 视口仍为 `1440 × 1024`；该差异属于浏览器截图宿主的栅格化，不改变页面布局测量。

### 正式前端运行态复核

在 `http://localhost:3000/` 打开正式编辑器并读取计算样式：

- 文档 Tab：`rgb(22, 70, 216) 2px 0 0 0 inset`。
- 边注面板 Tab：`rgb(22, 22, 22) 0 -2px 0 0 inset`。
- 页面控制台 `warn/error`：空。

截图：[M0-01-formal-editor-1440.png](<浏览器证据/M0-01-formal-editor-1440.png>)。

## Q-001～Q-004 状态

| 问题 | 本轮处理 | 当前状态 |
| --- | --- | --- |
| Q-001 208/232px 语义冲突 | 设计文档已修订；正式决策记录仍待规划与验收方落库。当前正式 Shell 仍把目录树和功能导航合在同一 `knowra-rail`，不能仅改一个宽度值冒充结构迁移完成。 | 待正式决策/结构迁移 |
| Q-002 220/232px 冲突 | 组件库目录示例已改为 232px；菜单仍保留 220px。 | 规格来源已修订，待正式决策落库 |
| Q-003 300px/232px 命名边界 | `InspectorWide`、`IndexInspector`、`MarginaliaPanel` 已在设计文档和组件库中分开命名。 | 规格来源已修订，待正式决策落库 |
| Q-004 原型浏览器视觉 QA | 三张目标原型截图和页面控制台结果已补齐，并与 M0-03 正式前端基线分开保存。 | 证据已补齐，待验收方复审 |

## 自动化验证

```text
git diff --check                 PASS
npm run test:web                 131 passed, 0 failed
```

## 角色边界

本轮没有修改正式迁移的 `00/01/02/03/05/06` 规划、契约、验收方案、决策记录或验收台账。正式决策记录中的 Q-001～Q-004 状态仍需由规划与验收方复审后更新。


# M0-01 规格消歧验收报告

## 验收信息

- 日期：2026-08-03
- 对应任务：M0-01 规格消歧
- 审查对象：`M0-01 规格消歧报告.md`
- 结论：`BLOCKED（仅剩 Q-004 浏览器视觉证据）`

## 已通过的报告质量检查

- 208px 功能导航、232px 页面辅助栏与 300px `InspectorWide` 的证据引用准确。
- 220px 目录示例与 232px 生产目录规格的冲突定位准确。
- 模块 Tab、文档 Tab、专注模式和 Shell Footer 的现状差异描述准确。
- Tab 激活线已按用户决定收敛：标签页使用左侧蓝线；模块、视图或面板 Tab 切换使用下方黑线。
- 报告遵守角色边界，没有修改正式前端、规划契约或正式决策记录。
- 未把静态源码检查冒充为浏览器视觉证据。
- `git diff --check` 通过。

## 未通过的准出项

### BLOCKED：Q-004 浏览器视觉证据缺失

- 当前报告只有 CSS/DOM 静态证据。
- 没有三张目标原型的本轮浏览器截图和控制台结果。
- 报告明确要求 Q-004 关闭后再申请正式 PASS。

## 已确认、待正式落库的规则

以下规则已按用户决定确认，仍需在最终收口时写入正式决策：

1. 全局功能导航固定为 208px。
2. 资料目录、索引详情和编辑器边注分别作为 232px 页面辅助栏。
3. 300px 只用于显式命名的 `InspectorWide`，不作为 M3/M4 默认侧栏。
4. 220px 保留为菜单或历史组件展示尺寸，不作为生产目录栏宽度。
5. 标签页使用左侧 2px 蓝色激活线；模块、视图或面板 Tab 切换使用黑色 2px 下边线和 700 字重。
6. Shell Footer 属于全局外壳，页面只注入状态内容。
7. 专注模式退出时恢复进入前的侧栏、面板 Tab 与滚动偏好。

## 重新验收条件

1. 规划与验收方将用户确认的 Tab 激活线规则及上述无异议建议写入决策记录和迁移契约。
2. 补齐 Q-004 浏览器截图与控制台证据，或明确将其转交 M0-03 且不作为 M0-01 门禁。
3. 同步修订仍冲突的风格文档、组件库说明和原型批次说明。

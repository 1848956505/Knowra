# M0-02 正式事件与状态映射验收报告

## 验收信息

- 首轮审查日期：2026-08-03
- 本次复验日期：2026-08-03
- 对应任务：M0-02 正式事件与状态映射
- 审查对象：`M0-02 正式事件与状态映射报告.md`
- 主验收者：Codex
- 本次复验方式：Codex 独立完成，未调用 Claude。
- 执行差异：仅修订映射报告和验收记录；未修改正式前端代码、API、迁移契约或决策记录；保留工作区原有改动。
- 首轮结论：`REJECTED`
- 本次结论：`PASS`

本结论针对 M0-02 的映射报告交付物，不代表当前正式前端实现或整个 M0 阶段已经完成验收。

## 验收依据

- `docs/前端重构/正式迁移/01-迁移契约与功能对等清单.md`：正式行为属性必须可追溯；视觉容器属性不能替代控制器 selector。
- `docs/前端重构/正式迁移/02-分阶段实施任务书.md:14-19`：M0-02 要提交 selector、controller、state、test 的完整映射报告，并确认正式任务不依赖原型 `data-hook`。
- `docs/前端重构/正式迁移/03-验收与回归方案.md:5-29`：按 P0/P1/P2 规则形成结论，并至少运行 `git diff --check` 与 `npm run test:web`。
- `docs/前端重构/正式迁移/03-验收与回归方案.md:45-53`：正式事件属性必须与控制器查询一致，且不得遗留原型 `data-hook`。
- `docs/前端重构/正式迁移/04-执行模型交接模板.md`：执行模型提交证据，规划与验收方负责最终结论。
- `docs/前端重构/正式迁移/05-决策与偏差记录.md`：本次没有新增偏差或改变冻结决策。

## 本次复验门禁

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `git diff --check` | PASS | 无输出、无空白错误 |
| `npm run test:web` | PASS | `Web tests: 131 passed, 0 failed (of 131)` |
| `rg -n 'data-hook|data-hook=' apps/web apps/web/test` | PASS | 无匹配 |
| 事件 binder selector 与修订报告对照 | PASS | `apps/web/lib/events` 与 `apps/web/src/controllers` 中的行为 selector 均可在修订报告中追溯 |
| 编辑器 bundle 构建 | 不适用 | 本次无 bundle 或编辑器代码变更 |

## 首轮退回项复验

修订报告已补齐首轮退回的映射缺口，并将“renderer 是否输出”“binder/controller”“state owner”“测试证据”分开记录：

| 退回项 | 修订后证据 | 结果 |
| --- | --- | --- |
| `[data-editor-cancel]` | 已补入树内联编辑的 renderer、`folder-tree-events.js` 取消分支、`treeEditor` state 和直接 binder 测试 | 已关闭 |
| `[data-editor-aside-toggle]` | 已补入 Shell 输出、`library-index-events.js` 分支、`view.showRightSidebar` state；同时明确直接 binder 测试缺口 | 已覆盖并登记后续测试 |
| `[data-create-question-from-objective]` | 已补入知识检查器 renderer、`work-domain-events.js` 与 `trainingWorkspaceController.createQuestion(objectiveId)`；直接 binder 测试缺口单独标记 | 已覆盖并登记后续测试 |
| 四类 `[data-inspector-kind]` | 已逐项补入 renderer、工作域 binder 的字段读取关系、state/controller 归属和测试现状 | 已覆盖并登记后续测试 |
| `aside-events/click.js:61-110` 动作组 | 已逐项标记当前 renderer 输出、历史/旧 selector、可达性、保留依据、controller/state 与测试状态 | 已关闭报告枚举缺口 |

修订报告没有把 renderer 可达性、controller 间接测试或 API 测试冒充为 binder 闭环测试；缺失的直接事件测试仍按路径单独列出并指定后续承接任务。

## 已登记的后续实现风险

下列问题仍存在于当前正式实现，但已经在 M0-02 修订报告中明确映射、标注证据状态并交给后续任务承接，不构成本次“映射报告交付物”的遗漏：

- 附件重命名 renderer/controller 已存在，但侧栏提交、输入和取消事件尚未完整接线。
- 索引详情的 `[data-attachment-open]` 有 renderer 输出，但尚无对应的 `library-index-events.js` 委托分支。
- 知识/训练工作域、编辑器正文和边注复杂动作仍缺少部分直接 binder 回归测试。
- 设置入口仍是占位路径，按既有规划不在本任务中虚构实现。

这些风险不能被本报告的 PASS 解读为已完成实现，也不能据此提前宣告 M0 或后续页面迁移通过；它们必须在对应实现任务和回归验收中继续处理。

## 验收检查

- selector → host/renderer → binder/controller → state → test 的映射链：PASS。
- 首轮遗漏的正式 selector 与边注旧/新动作：PASS，已逐项登记。
- 原型 `data-hook` 依赖：PASS，未发现匹配。
- 迁移契约、规划文档和决策记录：未修改。
- 视觉证据：不适用。本任务只修订静态映射报告，不修改 UI、DOM 结构或视觉样式。
- 可访问性检查：不适用。本任务未改变交互语义；键盘与语义门禁继续由对应实现任务验收。

## 缺陷与偏差

- P0：无。
- P1（M0-02 交付物）：无。现有实现风险已在“后续实现风险”中明确登记，不再作为报告枚举缺陷。
- P2：无。
- 偏差记录：无新增 D-xxx；未改变 `05-决策与偏差记录.md` 中的冻结决策。

## 结论与下一步

结论：`PASS`。

M0-02 修订后的正式事件与状态映射报告满足本任务的交付与验收要求，首轮报告枚举缺口已关闭。可进入下一任务的规划/执行流程，但不得把本结论扩展为 M0 阶段或当前正式前端实现整体通过。

下一步：继续处理 M0-03 基线证据，并按修订报告登记的承接关系，在 M3/M4/M5 等对应任务中补齐附件事件接线和直接 binder 回归测试；M0-01 的 Q-004 仍按其独立阻塞记录处理。

# M3-02 索引主区复验验收报告

## 1. 当前验收结论

`PASS`

- 验收日期：2026-08-08
- 验收方：Codex
- 对应任务：M3-02 索引主区
- 验收基线：当前 `codex/frontend-refactor` 工作区、[M3-02 修复与复验报告](<M3-02 修复与复验报告.md>)及本次真实 API/Web 运行态。
- 验收依据：[迁移契约与功能对等清单](<../../01-迁移契约与功能对等清单.md>)、[验收与回归方案](<../../03-验收与回归方案.md>)、M3-02 任务说明和 A-018 上一轮复验记录。

本轮已复验通过 A-018 退回的 P1-2 Tab 键盘不可达问题；P1-1、P2-1、P2-2、P2-3 均已关闭。资料索引的真实筛选、Tab 切换、目录范围、分页、空结果、打开返回、长内容承载及多视口表现均通过，满足 M3-02 验收门禁，本次允许解锁 M3-03。

## 2. 验收范围

- `#library-index-view` 索引主区宿主、标题、Tab、筛选器、列表、分页和空状态。
- `tablist`、`tab`、`tabpanel` 的选中状态、关联关系和面板切换。
- 真实类型/状态/时间筛选、清除筛选、空结果反馈和分页。
- 父目录选择后的后代资料可见性。
- 长标题、长路径、空结果和 1024×900、1280×900、1440×900、2048×900 下的页面级横向溢出。
- 列表资料的选择、预览和打开返回；M3-03 详情栏业务不在本次范围内。

## 3. 自动化与静态检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run test:web` | PASS | Web 140/140 |
| `npm run test:api` | PASS | API 210/210 |
| `npm run test:scripts` | PASS | 脚本 10/10 |
| `npm test` | PASS | API 210/210、Web 140/140、脚本 10/10 |
| `git diff --check` | PASS | 无空白错误 |
| 相关静态约束 | PASS | 未发现 `data-hook`、CDN、`alert/prompt/confirm` 遗留 |
| 运行态页面控制台 | PASS | `error` / `warning` 均为空 |

## 4. 浏览器运行态复验

### 4.1 上一轮退回项

- 清除筛选：PASS。选择无匹配的 `PDF 导入` 后，页面显示 0 条、“没有找到匹配条目”和“清除筛选”；点击后类型恢复为“全部”，列表恢复 10 条，清除按钮消失，页码回到第 1 页。
- 闭合筛选器控制目标：PASS。类型、状态、时间三个触发器均保留 `aria-controls`，闭合时对应 `role="menu"` 节点仍存在并带 `hidden`，打开后可见。

### 4.2 核心功能

- Tab 切换：PASS（功能与键盘）。`全部条目`、`最近`、`已收藏`、`回收站` 可切换，真实数据分别显示 17、5、0、0 条；空视图可显示无结果反馈。`ArrowRight`、`ArrowLeft`、`ArrowDown`、`ArrowUp`、`Home`、`End` 均能切换活动 Tab、同步焦点和面板标识。
- 类型/状态/时间筛选：PASS。类型菜单包含全部、手动笔记、Markdown 导入、PDF 导入、文件资料；状态和时间筛选可组合，筛选结果和排序随选择更新。
- 目录后代筛选：PASS。选择 `A股` 后显示该目录下的 3 条资料：`观点`、`codex推荐`、`A股短线 ChatGPT 使用模板`；返回全部范围后恢复全量列表。
- 真实分页：PASS（功能）。默认每页 10 条进入第 2 页显示 7 条；切换每页 5 条后回到第 1 页、共 4 页；第 4 页显示 2 条；恢复每页 10 条后回到默认承载。
- 选择、打开与返回：PASS。单击列表项可切换选中项；双击资料可进入编辑器；点击返回资料索引后回到索引页并保留资料库范围。
- 长内容与空结果：PASS。已有长标题/长路径承载未造成页面级横向溢出；空结果区域保留清除筛选入口。

### 4.3 多视口复核

| 视口 | 页面/状态 | 页面 `scrollWidth` | `body.scrollWidth` | 结果 |
| --- | --- | ---: | ---: | --- |
| 1024×900 | 全部资料、每页 10 条 | 1024 | 1024 | PASS |
| 1280×900 | 全部资料、每页 10 条 | 1280 | 1280 | PASS |
| 1440×900 | 全部资料、每页 10 条 | 1440 | 1440 | PASS |
| 2048×900 | 全部资料、每页 10 条 | 2048 | 2048 | PASS |

修复侧视觉证据：[空结果筛选](<浏览器证据/M3-02-repair-01-empty-filter-1280x720.png>)、[清除筛选后恢复列表](<浏览器证据/M3-02-repair-02-filter-cleared-1280x720.png>)、[Tab/tabpanel 与分页语义](<浏览器证据/M3-02-repair-03-tab-pagination-1280x720.png>)、[Tab 键盘导航](<浏览器证据/M3-02-repair-04-tab-keyboard-1280x720.png>)。

### 4.4 可访问性与结构

- 筛选菜单语义：PASS。打开态触发器具有 `aria-expanded`，菜单为 `role="menu"`，选项为 `role="menuitemradio"` 并同步 `aria-checked`；闭合态控制目标已修复。
- Tab/tabpanel 关联：PASS，P2-2 已关闭。`#library-index-tabs` 为 `role="tablist"`，四个 Tab 具备稳定 `id`、`role="tab"`、动态 `aria-selected`、`aria-controls` 和 roving `tabindex`；`#library-index-content` 为 `role="tabpanel"`，并在切换后同步 `aria-labelledby`。
- 分页选中状态：PASS，P2-3 已关闭。当前页输出 `aria-current="page"`，每页数量按钮输出正确的 `aria-pressed` 和中文 `aria-label`。
- Tab 键盘可达性：PASS，P1-2 已关闭。真实页面验证 `ArrowRight`、`ArrowLeft`、`ArrowDown`、`ArrowUp`、`Home`、`End`；每次操作均切换活动 Tab、将焦点转移到新的活动 Tab，并同步 `aria-selected`、`tabindex` 和 `tabpanel[aria-labelledby]`。

## 5. 缺陷、偏差与验收边界

- P0：无。
- P1：无；A-018 的 P1-2 已关闭。
- P2：无；P2-1、P2-2、P2-3 均已关闭。
- 已关闭问题：A-016 的 P1-1 清除筛选回归、P2-1 闭合筛选器 `aria-controls` 目标断裂、A-017 的 P2-2/P2-3，以及 A-018 的 P1-2。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 验收边界：验收方未修改正式前端实现；本次仅新增复验报告并更新正式验收记录。

## 6. 最终结论与下一步

M3-02 复验结论为 `PASS`。P1/P2 门禁均已通过，允许解锁 M3-03 资料详情栏。

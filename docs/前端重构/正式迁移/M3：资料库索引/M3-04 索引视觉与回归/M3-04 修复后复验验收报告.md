# M3-04「索引视觉与回归」修复后复验验收报告

## 1. 当前验收结论

`PASS`

- 验收日期：2026-08-08
- 验收方：Codex
- 对应任务：M3-04「索引视觉与回归」
- 复验基线：原 [M3-04 验收报告](<M3-04 验收报告.md>)、执行方 [M3-04 修复与复验报告](<M3-04 修复与复验报告.md>)、A-021 退回记录及当前真实 API/Web 运行态。
- 复验范围：A-021 登记的 `M3-04-P2-1` 焦点恢复缺陷，以及修复后完整自动化、四档视口和控制台回归。

本轮确认目录栏和详情栏关闭后焦点均返回可见的重开触发器，A-021 的唯一 P2 已关闭；原 M3-04 的视觉、功能和视口矩阵结论仍然成立，本次复验通过，允许解锁 M4-01。

## 2. 自动化与静态检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm test` | PASS | API 210/210、Web 140/140、脚本 10/10 |
| 详情/目录焦点事件回归 | PASS | 新增关闭路径焦点恢复断言通过 |
| `git diff --check` | PASS | 无空白错误 |

## 3. 真实运行态复验

### 3.1 M3-04-P2-1 焦点恢复

- 1280×720 真实资料索引中选中 `Transformer`，点击“收起详情”后：详情栏为 `data-open="false"`，焦点元素为原生 `BUTTON`，可访问名称为“展开详情”，`aria-expanded="false"`，`aria-controls="library-index-inspector"`。
- 同一运行态点击“折叠目录栏”后：Shell 为 `data-directory-hidden="true"`，焦点元素为 `#library-index-directory-reopen`，按钮可见，`aria-label="展开目录栏"`，`aria-expanded="false"`，`aria-controls="kb-sidebar"`。
- 两条关闭路径均在对应重绘完成后恢复焦点，未再落到 `BODY`；页面控制台 `error` / `warn` / `warning` 均为空。

### 3.2 四档视口矩阵

| 视口 | 目录栏 | 索引主区 | 详情栏 | `body/document.scrollWidth` | 结果 |
| --- | ---: | --- | --- | ---: | --- |
| 1024×768 | 232px | x=440，w=584；详情覆盖 | x=792，w=232 | 1024 / 1024 | PASS |
| 1280×720 | 232px | x=440，w=840；网格 608px + 232px | x=1048，w=232 | 1280 / 1280 | PASS |
| 1440×1024 | 232px | x=440，w=1000；网格 768px + 232px | x=1208，w=232 | 1440 / 1440 | PASS |
| 2048×1024 | 232px | x=440，w=1608；网格 1376px + 232px | x=1816，w=232 | 2048 / 2048 | PASS |

四档视口均无页面级横向溢出；修复只涉及事件焦点回收和回归断言，未改变 M3-04 已验收的视觉布局、数据链路和索引功能。

## 4. 缺陷、偏差与验收边界

- P0：无。
- P1：无。
- P2：无；A-021 的 `M3-04-P2-1` 已关闭。
- 偏差记录：1440×1024、2048×1024 执行截图文件仍存在像素尺寸与文件名不一致，已在原验收报告记录；本次以真实 DOM 视口矩阵作为尺寸证据，不影响产品功能验收。
- 验收边界：验收方未修改前端实现；本次仅新增修复后复验报告并更新正式验收记录。

## 5. 最终结论与下一步

M3-04 修复后复验结论为 `PASS`。焦点恢复、视觉矩阵、索引功能回归和控制台检查均通过，允许解锁 M4-01「编辑器网格与目录栏」。

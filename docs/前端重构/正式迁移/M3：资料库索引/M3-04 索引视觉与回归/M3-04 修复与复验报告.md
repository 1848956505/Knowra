# M3-04 修复与复验报告

## 1. 执行状态

- 对应任务：M3-04 索引视觉与回归
- 修复依据：[M3-04 验收报告](<M3-04 验收报告.md>)
- 当前状态：`执行侧完成 / 待验收方复审`
- 说明：本报告只记录执行方修复和验证，不替代验收报告，也不修改正式验收记录。

## 2. 本次修复

### M3-04-P2-1 折叠后焦点未返回重开触发器

- 详情栏关闭路径在索引内容重绘完成后，查询新生成的 `[data-index-inspector-open]` 按钮并恢复焦点。
- 目录栏关闭路径在 Shell 全量重绘完成后，聚焦缓存的 `#library-index-directory-reopen` 按钮。
- 仅在从打开状态切换为关闭状态时执行焦点回收，目录栏重开和详情栏展开的既有操作链路保持不变。
- 为两条关闭路径补充事件回归断言，确保焦点恢复发生在对应重绘之后。

## 3. 自动化验证

| 检查项 | 结果 |
| --- | --- |
| `npm test` | PASS，API 210/210、Web 140/140、脚本 10/10 |
| 详情/目录焦点事件回归 | PASS |
| `git diff --check` | PASS |

## 4. 真实运行态验证

- 1280×720 资料索引中关闭详情栏后，`#library-index-inspector[data-open="false"]`，可见重开按钮的 `aria-label` 为“展开详情”，且 `document.activeElement` 指向该按钮。
- 同一运行态关闭目录栏后，`data-directory-hidden="true"`，`#library-index-directory-reopen.hidden` 为 `false`，且 `document.activeElement.id` 为 `library-index-directory-reopen`。
- 目录栏折叠后页面 `body.scrollWidth` 与视口宽度均为 1280，无横向溢出。
- 浏览器运行态控制台 `error` / `warn` / `warning` 数量为 0。

## 5. 变更文件

- `apps/web/lib/events/library-index-events.js`
- `apps/web/test/events/library-index-events.test.js`
- 本报告及结构性文档同步记录。

## 6. 验收边界

本次未修改 M3-04 验收报告、`06-验收记录.md` 或后续任务解锁状态；当前仍为执行侧完成、待验收方复审，请验收方按原报告要求复验 M3-04-P2-1 后更新正式结论。

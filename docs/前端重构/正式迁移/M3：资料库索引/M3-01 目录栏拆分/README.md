# M3-01 目录栏拆分

## 任务状态

`执行侧完成 / 待正式验收`

## 前置条件

- M2-05 主页外壳验证已由正式验收记录 A-014 判定为 `PASS`。
- M2-03 已完成 208px 全局功能导航与 232px 资料库目录的物理三列布局。本任务继续收口目录宿主语义，不重复实现 M2 的功能导航。

## 任务范围

- 将 `#folder-tree` 的宿主显式标记为资料库索引页的 232px 目录栏。
- 保留 `#folder-tree` 这一稳定内联编辑宿主和既有 `element-cache` / `folder-tree-events` 接线。
- 保留文件夹选择、增删改、拖拽、右键菜单、分组折叠与文件夹展开/收起行为。
- 将目录栏显隐状态从通用 `sidebar` 语义切换为 `directoryBar`，同时保留兼容别名。

## 不在本任务范围

- 不迁移资料索引主区、筛选、列表、分页或详情检查器；这些属于 M3-02。
- 不改写导航 controller、tree renderer 或 `folder-tree-events.js` 的业务行为。
- 不修改正式迁移规划、验收结论或偏差记录。

## 输出

- [M3-01 执行报告](<M3-01 执行报告.md>)
- [M3-01 修复与复验报告](<M3-01 修复与复验报告.md>)

正式验收结论由验收方写入正式验收记录，不在本目录预先声明 `PASS`。

# 正式 UI 迁移验收记录

本文件只记录规划与验收方给出的正式结论。

## A-000 迁移准备基线

- 日期：2026-08-03
- 对应阶段：迁移前审查
- 范围：`docs/前端重构/` 设计文档、页面原型、组件库及 `apps/web` 正式前端底座。
- 自动化：`npm run test:web`，131 passed / 0 failed。
- 静态检查：`git diff --check` 通过；三个含脚本的当前原型页面脚本可解析。
- 已确认优势：视觉系统和核心页面目标基本完整；正式前端控制器、渲染器、状态和测试底座健康。
- P0：正式事件契约映射未建立；被引用的旧功能对等方案当前不存在。
- P1：原型无响应式/容器查询；原型交互语义不满足键盘要求；Token 和图标体系未对齐。
- 证据限制：最新原型 QA 记录仍因未完成浏览器视觉复核而标记 blocked。
- 结论：`REJECTED`（不准入正式实现，只准入 M0 规划收口）。
- 下一步：完成 M0-01、M0-02、M0-03 后复验。

## A-001 M0-01 规格消歧

- 日期：2026-08-03
- 对应任务：M0-01
- 静态检查：报告引用与设计文档、组件库和三张页面原型一致；`git diff --check` PASS。
- 已接受建议：208px 功能导航；232px 页面辅助栏；300px `InspectorWide`；黑线模块 Tab；全局 Shell Footer；可恢复的专注模式。
- P1：文档 Tab 顶部蓝线与最新编辑器原型左侧蓝线尚未裁决。
- 阻塞：Q-004 没有本轮浏览器截图和控制台证据。
- 证据：[M0-01 验收报告](<M0：契约与基线/M0-01 规格消歧/M0-01 验收报告.md>)。
- 结论：`BLOCKED`（报告质量通过，但尚未满足无冲突规格的准出条件）。
- 下一步：用户决定文档 Tab 激活线；关闭或转交 Q-004；规划方完成决策落库后复验。

## A-002 M0-02 正式事件与状态映射

- 日期：2026-08-03
- 对应任务：M0-02
- 执行差异：修订 `M0-02 正式事件与状态映射报告.md`；未修改正式前端代码、API、迁移契约或决策记录。
- 自动化：`git diff --check` PASS；`npm run test:web` PASS（131 passed / 0 failed）；`data-hook` 静态审计无匹配；事件 binder selector 与修订报告对照无遗漏。
- 视觉证据：不适用。本任务仅验收静态映射报告，未修改 UI、DOM 或样式。
- 功能检查：已补入首轮遗漏的 `data-editor-cancel`、`data-editor-aside-toggle`、`data-create-question-from-objective`、四类 `data-inspector-kind`，并逐项审计 `aside-events/click.js:61-110` 的新旧动作、可达性与保留依据。
- 可访问性检查：不适用。本任务未改变交互语义；相关键盘和语义门禁由后续实现任务验收。
- P0：无。
- P1：M0-02 映射报告交付物无；附件事件未完整接线、部分直接 binder 测试缺口已在报告中登记为后续实现风险。
- P2：无。
- 偏差记录：无新增 D-xxx。
- 结论：`PASS`
- 下一步：接受 M0-02 修订报告；继续 M0-03，并按承接任务处理附件接线与 binder 回归测试；M0-01 的 Q-004 仍保持独立阻塞状态。

## A-003 M0-03 迁移前基线证据

- 日期：2026-08-03
- 对应任务：M0-03
- 执行差异：审查既有 M0-03 基线报告、截图索引和 21 张 JPEG 原件；新增 M0-03 验收报告，未修改正式前端、API 或基线原件。
- 自动化：`npm run test:web` PASS（131 passed / 0 failed）；`git diff --check` PASS；M0-03 目录空白扫描 PASS；截图引用 21/21 与原件一致。
- 视觉证据：2048×1024、1440×1024、1280×720 三档主矩阵齐全；2048×1024 覆盖编辑器四种侧栏状态；1024×768 补充默认入口、资料索引和覆盖式面板。
- 功能检查：正式 API/Web 真实数据基线已记录首页边界、资料索引、编辑器、知识和训练工作域；报告记录了每组视口的数据量、面板状态、差异标识和页面控制台结果。
- 可访问性检查：不作实现通过结论；本任务仅冻结迁移前证据，截图不替代键盘、屏幕阅读器或 200% 缩放门禁。
- P0：无。
- P1：无。
- P2：无验收阻塞项；截图栅格限制、默认首页边界和真实数据变化已作为基线约束登记。
- 偏差记录：无新增 D-xxx。
- 结论：`PASS`
- 下一步：接受 M0-03 基线证据，后续迁移沿用同一差异标识和状态条件；M0-01 的 Q-004 仍保持独立阻塞状态。

## A-004 M1-01 InkGrid Token 接入

- 日期：2026-08-04
- 对应任务：M1-01
- 执行差异：审查 M1-01 执行报告、Token 验证证据、Token 文件及关联 CSS/测试改动；未修改正式代码。
- 自动化：Token 形状测试 PASS；`npm run test:web` PASS（132 passed / 0 failed）；`npm run test:api` PASS（210 passed）；脚本测试 PASS（10）；在允许本地监听后 `npm test` 全部通过；`git diff --check` PASS。
- 视觉证据：两张 `1280×720` JPEG 有效；首页和编辑器可加载，Token computed style 与激活线证据已提供。
- 功能检查：兼容别名和 Token 形状通过；未发现新增组件 CSS 硬编码视觉值。
- 可访问性检查：不作通过结论；本任务未改变交互语义，但现有 Token/样式范围问题需先收口。
- P0：无。
- P1：执行报告漏列 5 个实际生产 CSS 改动，范围扩展未登记；canonical 字体/字号 Token 与当前 InkGrid 设计基线不一致，且无批准偏差。
- P2：无。
- 偏差记录：无已批准 D-xxx；P1 问题不得视为已批准偏差。
- 结论：`REJECTED`
- 下一步：退回 M1-01 执行方补齐范围说明、核对并修正/登记 Token 基线偏差、补强值域测试后复验；暂不解锁 M1-02/M1-03。

## A-005 M1-01 InkGrid Token 接入复验

- 日期：2026-08-04
- 对应任务：M1-01
- 执行差异：复验修复与复验报告、更新后的执行报告、Token 验证证据、Token 文件及关联 CSS/测试改动；保留 A-004 的 `REJECTED` 历史记录，未修改正式实现。
- 自动化：`node apps/web/test/inkgrid-token-shape.test.js` PASS；`npm run test:web` PASS（132 passed / 0 failed）；`npm run test` PASS（API 210、Web 132、脚本 10/10；Git Bash PATH 前提已满足）；`git diff --check` PASS。
- 视觉证据：1280×720 首页和编辑器 JPEG 可加载；修复后 computed style 记录与设计基线一致，未见页面级横向溢出。
- 功能检查：`--ink-*` 分类、旧变量兼容别名、线条/阴影/尺寸/语义色及值域测试通过；5 个生产 CSS Token 消费点已补充披露并有 M0-01 冻结规则依据。
- 可访问性检查：不作本任务的独立通过结论；本任务未改变交互语义。
- P0：无。
- P1：无。A-004 的 P1-1 未披露范围和 P1-2 排印基线偏差均已收口。
- P2：无。
- 偏差记录：无已批准 D-xxx。
- 结论：`PASS`
- 下一步：解锁 M1-02、M1-03；页面结构宽度和完整视觉迁移按后续任务单独验收。

## A-006 M1-02 图标本地化与映射

- 日期：2026-08-04
- 对应任务：M1-02
- 执行差异：复核 M1-02 执行报告、图标映射证据及 faef0fd/e8b8e8c 实现；e8b8e8c 已收口初次验收发现的 3 处 Unicode ›，未修改正式实现。
- 自动化：icon-mapping 测试 PASS；npm test 全部通过（API 210、Web 133、脚本 10/10）；Editor bundle 构建 PASS；git diff --check PASS。
- 视觉证据：本任务不改变布局；独立本地静态服务核验 53/53 个映射 SVG 均返回 200 image/svg+xml，重建 bundle 后表格/图片图标使用中央映射。
- 功能检查：64 个语义名称统一映射到 53 个本地 Remix SVG；正式渲染器无 CDN、旧 Phosphor、直接资源路径、复制 UI SVG path 或 Unicode 图标字符；既有事件属性和控制器边界保持不变。
- 可访问性检查：装饰图标为 aria-hidden=true；交互按钮保留中文 aria-label/title。
- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增、无未批准偏差；用户图片占位 SVG 和 JSX 探索目录按既定边界排除。
- 结论：PASS
- 下一步：解锁 M1-03；旧图标资产保留到 M6 废弃资产审计。

## A-007 M1-03 InkGrid 样式入口

- 日期：2026-08-04
- 对应任务：M1-03
- 执行差异：审查当前工作区未提交的 M1-03 CSS 入口、五个 canonical 模块、兼容代理、测试与执行证据；未修改正式实现。
- 自动化：入口图测试 PASS（29 个样式文件，无缺失/循环）；`components-css-imports.test.js` PASS（30 项导入、28 个组件模块）；`npm run test:web` PASS（134）；允许本地监听后 `npm test` PASS（API 210、Web 134、脚本 10）；`git diff --check` PASS。
- 视觉证据：两张 1280×720 JPEG 原件有效，首页和编辑器在现有证据中正常渲染且无横向溢出；未提供 1024×768 最小支持布局证据，截图也未记录提交/工作区差异标识。
- 功能检查：主入口已切换到 `knowra-inkgrid.css`，导入图无缺失/循环，五个职责模块及旧兼容代理关系成立；但媒体查询中的 `.knowra-production-shell :root` 不会匹配普通文档根 `html`，导致 1180px 以下三个宽度变量不覆盖。
- 可访问性检查：本任务未改变 DOM、控制器或事件语义，不作独立键盘、屏幕阅读器或 200% 缩放通过结论；响应式布局回归需先修复。
- P0：无。
- P1：`knowra-inkgrid-library-index.css:161` 的响应式 Token 覆盖选择器失效，造成最小支持布局回归。
- P2：无。
- 偏差记录：无新增、无已批准 D-xxx；1024×768 运行态及截图差异标识证据不足，需随修复复验补齐。
- 结论：`REJECTED`
- 下一步：退回执行方修复响应式变量作用域，补充回归断言、1024×768 证据和工作区/提交差异标识后复验；在复验通过前不宣布 M1-03 完成。

## A-008 M1-03 InkGrid 样式入口复验

- 日期：2026-08-05
- 对应任务：M1-03
- 执行差异：复验 A-007 退回修复后的当前未提交工作区；确认响应式变量已改为 `.knowra-production-shell` 自身声明，未修改正式实现。
- 自动化：入口图测试 PASS（29 个样式文件，无缺失/循环，新增响应式作用域断言通过）；`components-css-imports.test.js` PASS（30 项导入、28 个组件模块）；`npm run test:web` PASS（134）；`npm test` PASS（API 210、Web 134、脚本 10）；`git diff --check` PASS。
- 视觉证据：1280×720 首页/编辑器及 1024×768 首页/索引/编辑器相关状态 JPEG 原件均有效；1024×768 三个宽度变量均为 `240px`，scroll width 为 `1024`，截图记录真实数据、页面/面板状态和统一源码集合差异标识。
- 功能检查：主入口、五个 canonical 模块、旧兼容代理、导入图和 Shell 作用域均通过；A-007 的 `.knowra-production-shell :root` 响应式回归已关闭；工程变更日志与项目结构导航同步内容属于规范要求的配套文档。
- 可访问性检查：本任务未改变 DOM、控制器或事件语义，不作独立键盘、屏幕阅读器或 200% 缩放通过结论；未发现新的交互语义风险。
- P0：无。
- P1：无；A-007 的响应式变量作用域问题已修复并有源码及运行态证据。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx；M0-01 的 Q-004 仍独立记录。
- 结论：`PASS`
- 下一步：解锁 M2-01；后续继续沿用当前入口、Token 唯一来源、Shell 作用域和本地资源约束。

## A-009 M2-01 AppShell 网格

- 日期：2026-08-06
- 对应任务：M2-01 AppShell 网格
- 执行差异：复验 `a6208b7 fix: complete M2-01 responsive shell layout` 及其修复与复验报告；未修改正式前端代码，新增当前验收运行的浏览器证据并覆盖 M2-01 原验收报告。
- 自动化：AppShell 专项测试 PASS；`npm run test:web` PASS（135/135）；`npm run test:api` PASS（210/210）；`npm run test:scripts` PASS（10/10）；`npm test` PASS；`git diff --check` PASS。
- 视觉证据：完成 2048×1024、1440×1024、1280×720、1024×768 视口复验；覆盖资料索引、编辑器、知识、训练，以及 2048 编辑器四种侧栏状态。所有截图均记录真实后端数据、页面/面板状态和 `a6208b7` 差异标识。
- 功能检查：TopBar 为 60px；StatusBarHost 为 32px、`z-index: 25` 且固定在页面底部；各抽查状态页面级 `scrollWidth/scrollHeight` 与视口一致；1024×768 ShellBody 为 `240px + 784px`，前次 ShellBody 错序/宽度 P1 已关闭；编辑器紧凑宽度 P1 已关闭。
- 宿主检查：SSR 初始工作区脚本、上下文菜单宿主、Markdown 导入 input、既有正式 ID 和 module client script 均保留；运行态无控制台 `error`/`warning`。
- 可访问性检查：本任务未改变事件、DOM 交互语义或状态契约；已检查顶栏、导航、编辑器和工作域的可见语义。截图不替代屏幕阅读器和完整 200% 缩放审计，相关门禁由对应功能任务继续承担。
- P0：无。
- P1：无；前次 M2-01 退回项均已修复并复验通过。
- P2：无新的 M2-01 阻塞项；资料索引内部详情列的后续响应式安排归入 M3 任务范围。
- 偏差记录：无新增、无已批准或未批准 D-xxx；M0-01 的 Q-004 仍为独立记录。
- 结论：`PASS`
- 下一步：解锁 M2-02 TopBar 与全局搜索；M2-01 后续不再退回执行。

## A-010 M2-02 TopBar 与全局搜索复验

- 日期：2026-08-06
- 对应任务：M2-02 TopBar 与全局搜索
- 执行差异：复验 `5eb26c6 fix(web): close search panel after navigation` 及其新增 controller/renderer 回归测试；未修改正式前端代码，仅新增验收侧浏览器截图并更新验收文档。
- 自动化：`npm run test:web` PASS（137/137）；允许本地监听后 `npm test` PASS（API 210、Web 137、脚本 10/10）；`git diff --check` PASS。
- 视觉证据：1440×1024 关键词 Enter 后编辑器截图、1024×768 搜索打开截图有效；1280×720、1440×1024、1024×768 运行态均无页面级横向溢出，TopBar 均为 60px；控制台 error/warning 为空。
- 功能检查：鼠标点击、标签筛选、`Ctrl+K`、`Meta+K`、Escape、关键词有结果 Enter、Enter 后 Escape、无结果 Enter、占位按钮均通过；中文输入法组合态由现有自动化测试覆盖通过。初验 P1-1 已关闭，关键词保留时关闭搜索不再渲染 `.search-panel`。
- 可访问性检查：搜索输入和占位按钮具备中文可访问名称；焦点、原生按钮、快捷键和 Escape 路径通过；屏幕阅读器与 200% 缩放不由本任务截图替代。
- P0：无。
- P1：无；A-010 关闭初验报告中的 P1-1。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx；M0-01 的 Q-004 仍为独立记录。
- 结论：`PASS`
- 下一步：解锁 M2-03 纯功能导航；继续保持 M2-03/M2-04 的范围边界，不提前拆分 Footer 或业务状态。

## A-011 M2-03 纯功能导航

- 日期：2026-08-06
- 对应任务：M2-03 纯功能导航
- 执行差异：审查当前未提交的 M2-03 工作区实现、执行报告、专项测试和浏览器证据；验收方未修改正式前端代码，新增验收报告和验收侧截图。
- 自动化：`npm run test:web` PASS（137/137）；允许本地监听后 `npm test` PASS（API 210、Web 137、scripts 10/10）；`git diff --check` PASS。
- 视觉证据：1440×1024、1280×720、1024×768 资料索引及 1024×768 知识库运行态均通过；Grid 分别为 `208/232/1000`、`208/232/840`、`208/232/584`、`208/816`，页面无横向溢出；控制台 error/warning 为空。
- 功能检查：资料库、知识库、训练场、学习档案可切换且激活态正确；非资料工作域隐藏 232px 资料目录；返回资料库恢复目录；15 个占位入口均为 `disabled + aria-disabled`；设置仅保留在 TopBar；专注模式边界通过。
- 可访问性检查：功能导航使用原生 nav/section/h2/button，工作域使用 `aria-current`，占位项和图标语义通过；屏幕阅读器与 200% 缩放不由本任务截图替代。
- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx；旧 Web 进程仅作为环境问题排除，未计入验收结论。
- 结论：`PASS`
- 下一步：解锁 M2-04 StatusBarHost；保持 Footer 内容迁移和占位业务能力不提前进入 M2-03。

## A-012 M2-04 StatusBarHost

- 日期：2026-08-06
- 对应任务：M2-04 StatusBarHost
- 执行差异：审查当前未提交的 M2-04 工作区实现、执行报告、专项测试和浏览器证据；验收方未修改正式前端代码，新增 M2-04 验收报告及验收侧截图。
- 自动化：npm run test:web PASS（137/137）；允许本地监听后 npm test PASS（API 210、Web 137、scripts 10/10）；git diff --check PASS。
- 视觉证据：1440×1024 索引与编辑器、1280×720 编辑器、1024×768 知识与训练工作域均完成当前运行态复核；外层 StatusBarHost 均为 32px 且页面无横向溢出，但编辑器功能槽和状态按钮实测为 41.796875px，被 32px 宿主的 overflow:hidden 裁切；控制台 error/warning 为空。
- 功能检查：功能槽/全局槽分离、编辑器正式状态属性、索引/工作域 action 隔离、边注单次切换和专注进入/退出通过；编辑器切换到其他工作域后仍残留旧文档状态消息。
- 可访问性检查：原生 button 和中文 aria-label 保留；但状态按钮的可见边界被裁切，需修复后再完成焦点可见性复验；屏幕阅读器与 200% 缩放不由本任务截图替代。
- P0：无。
- P1：编辑器状态控制超过 32px Footer 宿主并被裁切；通用 status-action 的垂直 padding 未被 InkGrid 编辑器规则覆盖，违反 Footer 内容正确性门禁。
- P2：跨工作域切换后功能状态消息残留旧编辑器文档上下文。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`REJECTED`
- 下一步：退回执行方修复状态控制盒模型和工作域状态消息上下文；复验通过前不解锁 M2-05。

## A-013 M2-04 StatusBarHost 返修复验

- 日期：2026-08-06
- 对应任务：M2-04 StatusBarHost
- 执行差异：复验 A-012 退回项修复后的当前未提交工作区；状态 action 已固定为 28px、零垂直 padding，工作域切换已补充当前功能状态消息；验收方未修改正式前端代码，新增 [M2-04 复验验收报告](<M2：公共外壳与主页/M2-04 StatusBarHost/M2-04 复验验收报告.md>)及验收侧截图。
- 自动化：npm run test:web PASS（138/138）；允许本地监听后 npm test PASS（API 210、Web 138、scripts 10/10）；git diff --check PASS；data-hook 静态扫描无匹配。
- 视觉证据：1280×720 编辑器复验显示 Footer/功能槽均为 32px，4 个状态控件均为 28px 且完整位于宿主内；1440×1024 编辑器与索引、1024×768 知识库均无页面级横向溢出；控制台 error/warning 为空。
- 功能检查：编辑器功能状态与全局连接状态分槽通过；边注单次切换、专注进入/退出通过；从编辑器切换到资料库、知识、训练、学习后分别显示当前工作域消息，旧文档消息残留已关闭。
- 可访问性检查：原生 button、data-save-now、data-status-action 和中文 aria-label 保留；状态控件完整落在 Footer 宿主内；屏幕阅读器与 200% 缩放不由本任务截图替代。
- P0：无。
- P1：无；A-012 Footer 状态控件裁切问题已关闭。
- P2：无；A-012 工作域状态消息上下文残留问题已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`PASS`
- 下一步：解锁 M2-05 主页外壳验证。

## A-014 M2-05 主页外壳验证

- 日期：2026-08-07
- 对应任务：M2-05 主页外壳验证
- 执行差异：审查当前未提交的 M2-05 工作区实现、执行报告、浏览器验证证据和专项测试；验收方未修改正式前端代码，新增 M2-05 验收报告、验收侧浏览器矩阵截图和本条记录。
- 自动化：npm run test:web PASS（140/140）；npm test PASS（API 210/210、Web 140/140、脚本 10/10）；git diff --check PASS。
- 视觉证据：完成 2048×1024、1440×1024、1280×720、1024×768 主页矩阵；主页内部滚动可达工作域卡和最近编辑表，页面级横向滚动宽度与视口一致；Footer 均为 32px；1280×720 资料入口实测资料目录 232px；控制台 error/warning 为空。
- 功能检查：主页仅提供资料工作台主入口；资料入口进入正式索引；最近编辑 Transformer 复用正式资料选择链路进入编辑器并可返回主页；知识/训练显示真实后端工作域数据；学习档案保持 LearningEvidence 与 MasteryState 依赖门禁；工作域可通过正式导航返回主页或资料索引；无虚构待办、掌握度、复习或同步统计。
- 可访问性检查：主页入口、最近编辑和功能导航均使用原生 button，功能导航保留 nav/section/h2 语义与工作域 aria-current；1024×768 下功能导航内部纵向滚动后学习档案入口可见可达。本任务截图不替代屏幕阅读器和完整 200% 缩放审计。
- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx；M0-01 的 Q-004 仍为独立记录。
- 结论：PASS
- 下一步：解锁 M3-01 目录栏拆分。

## A-015 M3-01 目录栏拆分

- 日期：2026-08-07
- 对应任务：M3-01 目录栏拆分
- 执行差异：复验当前未提交工作区的 M3-01 实现、执行报告和修复与复验报告；上一轮目录树可访问性 P2 已补齐 `aria-expanded`、`aria-controls` 和稳定控制目标；验收方未修改正式前端代码，新增 [M3-01 验收报告](<M3：资料库索引/M3-01 目录栏拆分/M3-01 验收报告.md>)。
- 自动化：`npm run test:web` PASS（Web 140/140）；`npm run test:api` PASS（API 210/210）；`npm run test:scripts` PASS（10/10）；`git diff --check` PASS。
- 视口和状态：1024×768、1280×720、1440×1024、2048×1024 下目录栏均为 232px，位于 x=208，页面无横向溢出；真实运行态控制台 error/warning 为空。
- 功能检查：目录选择、分组折叠、文件夹展开/收起、右键菜单、临时目录创建/重命名/删除、内联编辑和目录头部辅助菜单均通过；拖拽由 `navigation-drag-drop` 与 `folder-tree-events` 自动化回归覆盖通过。
- 可访问性检查：分组和有子内容的文件夹按钮均具备同步的 `aria-expanded`、`aria-controls`，控制目标存在且折叠时使用 `hidden`；目录宿主保留中文 `aria-label`，图标使用 `aria-hidden`。屏幕阅读器与完整 200% 缩放审计不由本次运行态替代。
- P0：无。
- P1：无。
- P2：无；上一轮目录树折叠语义 P2 已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx；M0-01 的 Q-004 仍为独立记录。
- 结论：`PASS`
- 下一步：解锁 M3-02 索引主区；继续保持 M3-01 的目录栏与 M3-02 索引主区范围边界。

## A-016 M3-02 索引主区

- 日期：2026-08-07
- 对应任务：M3-02 索引主区
- 执行差异：审查当前未提交工作区的 M3-02 实现、执行报告和真实运行态；验收方未修改正式前端代码，新增 [M3-02 验收报告](<M3：资料库索引/M3-02 索引主区/M3-02 验收报告.md>)。为验证分页、目录后代筛选和长内容，临时创建 1 个长目录与 12 条资料，验收后已永久清理，API 恢复为原始 2 条资料、0 个验收目录。
- 自动化：`npm run test:web` PASS（Web 140/140）；`npm run test:api` PASS（API 210/210）；`npm run test:scripts` PASS（10/10）；`git diff --check` PASS。
- 视口和状态：真实 API/Web 下完成 1024×900、1280×900、1440×900、2048×900 索引矩阵；长标题、长路径均未造成页面级横向溢出；运行态控制台 `error` / `warning` 为空。
- 功能检查：Tab/tabpanel、真实分页、收藏/回收站空结果、父目录后代资料筛选、长标题/路径承载和资料打开/返回通过；选择无匹配类型后点击“清除筛选”，类型仍为 `PDF 导入` 且结果仍为 0 条，清除筛选功能未恢复，判定 P1。
- 可访问性检查：Tab/tabpanel 关联和打开态筛选菜单语义通过；筛选器闭合时仍带 `aria-controls`，但对应菜单节点不存在，判定未登记 P2。
- P0：无。
- P1：1 项；清除筛选后类型条件未清除，用户无法从空结果恢复全部资料。
- P2：1 项；筛选器闭合态 `aria-controls` 指向不存在的控制目标，未登记或批准。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`REJECTED`
- 下一步：执行方修复 P1-1 并处理/登记 P2-1；复验通过前不解锁 M3-03。

## A-017 M3-02 索引主区复验

- 日期：2026-08-07
- 对应任务：M3-02 索引主区
- 执行差异：复验当前未提交工作区的 M3-02 修复与复验报告、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，新增 [M3-02 复验验收报告](<M3：资料库索引/M3-02 索引主区/M3-02 复验验收报告.md>)。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；`git diff --check` PASS；相关静态约束检查 PASS。
- 视觉证据：复核 [空结果筛选](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-01-empty-filter-1280x720.png>) 和 [清除筛选后恢复列表](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-02-filter-cleared-1280x720.png>)；真实运行态补测 1024×900、1280×900、1440×900、2048×900，四档页面级宽度均与视口一致。
- 功能检查：A-016 的 P1-1 清除筛选回归已关闭；P2-1 闭合筛选器控制目标已关闭；类型/状态/时间筛选、Tab 切换、父目录后代筛选、真实分页、空结果、资料打开与返回通过。
- 可访问性检查：筛选菜单控制目标与打开态菜单语义通过；Tab/tabpanel 关联和选中状态未满足契约，判定 P2-2；分页当前页和每页数量仅使用 `data-active`，未暴露 `aria-current` / `aria-pressed`，判定 P2-3。
- P0：无。
- P1：无；P1-1 已关闭。
- P2：2 项；P2-2、P2-3 均未登记负责人、后续任务或批准偏差。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`REJECTED`
- 下一步：补齐 Tab/tabpanel 与分页状态语义并补充回归断言；复验通过或取得正式偏差批准前不解锁 M3-03。

## A-018 M3-02 索引主区再次复验

- 日期：2026-08-07
- 对应任务：M3-02 索引主区
- 执行差异：继续复验当前工作区的 M3-02 修复与复验报告、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，更新 [M3-02 复验验收报告](<M3：资料库索引/M3-02 索引主区/M3-02 复验验收报告.md>)并新增本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；`git diff --check` PASS；相关静态约束检查 PASS。
- 视觉证据：复核 [空结果筛选](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-01-empty-filter-1280x720.png>)、[清除筛选后恢复列表](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-02-filter-cleared-1280x720.png>)和 [Tab/tabpanel 与分页语义](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-03-tab-pagination-1280x720.png>)；真实运行态补测 1024×900、1280×900、1440×900、2048×900，四档页面级宽度均与视口一致。
- 功能检查：A-016 的 P1-1 清除筛选回归、P2-1 闭合筛选器目标问题已关闭；P2-2 Tab/tabpanel 关联、P2-3 分页 ARIA 状态已关闭；类型/状态/时间筛选、Tab 视图切换、父目录后代筛选、真实分页、空结果、资料选择、打开与返回均通过。
- 可访问性检查：筛选菜单语义、Tab/tabpanel 关系和分页状态通过；但 Tab 使用 roving `tabindex` 后，真实页面聚焦“全部条目”按 `ArrowRight` 不会切换，其他 Tab 为 `tabindex="-1"` 且无方向键/Home/End 处理，判定新的 P1-2“索引 Tab 键盘不可达”。运行态页面控制台 `error` / `warning` 为空。
- P0：无。
- P1：1 项；P1-2 索引 Tab 缺少方向键/Home/End 切换，非当前 Tab 对键盘用户不可达。
- P2：无；P2-2、P2-3 已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`REJECTED`
- 下一步：执行方修复并登记 P1-2，补充 Tab 键盘交互回归；复验通过前不解锁 M3-03。

## A-019 M3-02 索引主区修复后复验

- 日期：2026-08-08
- 对应任务：M3-02 索引主区
- 执行差异：复验当前工作区的 M3-02 P1-2 修复、执行侧复验材料、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，更新 [M3-02 复验验收报告](<M3：资料库索引/M3-02 索引主区/M3-02 复验验收报告.md>)并新增本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；`git diff --check` PASS；相关静态约束检查 PASS。
- 视觉证据：复核 [空结果筛选](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-01-empty-filter-1280x720.png>)、[清除筛选后恢复列表](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-02-filter-cleared-1280x720.png>)、[Tab/tabpanel 与分页语义](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-03-tab-pagination-1280x720.png>)和 [Tab 键盘导航](<M3：资料库索引/M3-02 索引主区/浏览器证据/M3-02-repair-04-tab-keyboard-1280x720.png>)；真实运行态补测 1024×900、1280×900、1440×900、2048×900，页面级宽度均与视口一致。
- 功能检查：A-018 的 P1-2 已关闭；`ArrowRight`、`ArrowLeft`、`ArrowDown`、`ArrowUp`、`Home`、`End` 均能切换活动 Tab、转移焦点并同步面板；清除筛选、类型/状态/时间筛选、父目录后代筛选、真实分页、空结果、资料选择、打开与返回均通过；长标题承载与页面级横向溢出检查通过。
- 可访问性检查：Tab/tabpanel 关系、筛选菜单目标、分页 `aria-current` / `aria-pressed` 及 Tab 键盘导航均通过；运行态页面控制台 `error` / `warning` 为空。
- P0：无。
- P1：无；A-018 的 P1-2 已关闭。
- P2：无；P2-1、P2-2、P2-3 均已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`PASS`
- 下一步：解锁 M3-03 资料详情栏。

## A-020 M3-03 资料详情栏验收

- 日期：2026-08-08
- 对应任务：M3-03 资料详情栏
- 执行差异：复验当前工作区的 M3-03 执行报告、实现结构、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，新增 [M3-03 验收报告](<M3：资料库索引/M3-03 资料详情栏/M3-03 验收报告.md>)并新增本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；`npm run test:api`、`npm run test:web`、`npm run test:scripts` 均通过；`git diff --check` PASS。
- 视觉证据：复核 [2048×1024](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-2048x1024.png>)、[1440×1024](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-1440x1024.png>)、[1280×720](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-1280x720.png>)、[1024×768](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-1024x768.png>)、[滚动详情](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-1280x720-scroll.png>)和[折叠详情](<M3：资料库索引/M3-03 资料详情栏/浏览器证据/M3-03-1280x720-collapsed.png>)；真实运行态四档视口详情栏均为 232px，页面级宽度与视口一致。
- 功能检查：真实资料字段、目录范围选择、资料选择同步、内容大纲展开、独立滚动（`scrollTop` 0→580）、详情栏关闭/重开、资料打开与返回均通过；1024×768 覆盖层关闭/重开后仍保持 232px 和无页面横向溢出。
- 可访问性检查：详情栏宿主 `aria-label`、折叠/重开按钮的 `aria-controls` / `aria-expanded` / 中文名称、原生 `details/summary` 分组和可聚焦入口均通过；运行态控制台 `error` / `warn` / `warning` 为空。640px 窄 CSS 视口暴露全局外壳 1024px 最小宽度，该既有响应式边界未纳入本任务缺陷。
- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增、无已批准或未批准 D-xxx；附件暂无真实样本，未执行实际打开动作，渲染和事件委托由自动化覆盖。
- 结论：`PASS`
- 下一步：解锁后续资料库索引迁移任务。

## A-021 M3-04 索引视觉与回归验收

- 日期：2026-08-08
- 对应任务：M3-04 索引视觉与回归
- 执行差异：复验当前工作区的 M3-04 执行报告、原型对比证据、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，新增 [M3-04 验收报告](<M3：资料库索引/M3-04 索引视觉与回归/M3-04 验收报告.md>)并新增本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；`npm run test:api`、`npm run test:web`、`npm run test:scripts` 均通过；`git diff --check` PASS。
- 视觉证据：1280×720 原型与正式实现的主结构、点阵背景、标题/Tab/筛选/列表和方形控件语言整体对齐；真实运行态 1024×768、1280×720、1440×1024、2048×1024 的目录栏、索引主区、详情栏尺寸和页面宽度均通过。执行证据中 1440 图片实际为 1440×944、2048 图片实际为 1805×1024，已改以 DOM 矩阵作为两档尺寸的主要证据。
- 功能检查：本地搜索不污染全局搜索、Tab 方向键/Home/End、筛选空结果与清除、真实分页、目录折叠/重开、资料选择、双击打开/返回、详情折叠/重开和真实标签展示均通过。
- 可访问性检查：Tab/tabpanel、分页 `aria-current` / `aria-pressed`、目录/详情 `aria-controls` / `aria-expanded` 和中文可访问名称通过；但目录栏和详情栏折叠后焦点均落到 `BODY`，未返回重开触发器，判定 M3-04-P2-1。
- P0：无。
- P1：无。
- P2：1 项；M3-04-P2-1 折叠后的焦点未返回重开触发器。
- 偏差记录：无新增、无已批准或未批准 D-xxx；执行截图存在两档文件尺寸与视口命名不一致，已在验收报告记录。
- 结论：`REJECTED`
- 下一步：执行方修复焦点恢复并补充回归断言；修复后重新复验 M3-04，未通过前不解锁后续迁移任务。

## A-022 M3-04 索引视觉与回归修复后复验

- 日期：2026-08-08
- 对应任务：M3-04 索引视觉与回归
- 执行差异：复验执行方 [M3-04 修复与复验报告](<M3：资料库索引/M3-04 索引视觉与回归/M3-04 修复与复验报告.md>)、焦点修复实现、事件回归断言和真实 API/Web 运行态；验收方未修改正式前端代码，新增 [M3-04 修复后复验验收报告](<M3：资料库索引/M3-04 索引视觉与回归/M3-04 修复后复验验收报告.md>)并新增本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 140/140、脚本 10/10）；详情/目录焦点事件回归 PASS；`git diff --check` PASS。
- 功能检查：A-021 的 M3-04-P2-1 已关闭；详情栏关闭后焦点返回“展开详情”按钮，目录栏关闭后焦点返回 `#library-index-directory-reopen`；原 M3-04 的本地搜索、Tab 键盘导航、筛选/清除、分页、目录和详情折叠、资料打开返回、真实标签及四档视口矩阵保持通过。
- 可访问性检查：详情和目录重开按钮的中文名称、`aria-expanded`、`aria-controls` 与焦点恢复均通过；运行态控制台 `error` / `warn` / `warning` 为空。
- 视觉证据：四档真实 DOM 矩阵仍为 232px 目录/详情栏，`body/document.scrollWidth` 与视口一致；1440、2048 执行截图尺寸偏差仍保留在原验收报告中，未影响本次运行态尺寸复验。
- P0：无。
- P1：无。
- P2：无；A-021 的 M3-04-P2-1 已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`PASS`
- 下一步：解锁 M4-01 编辑器网格与目录栏。

## A-023 M4-01 编辑器网格与目录栏验收

- 日期：2026-08-09
- 对应任务：M4-01 编辑器网格与目录栏
- 执行差异：审查当前工作区的 M4-01 执行报告、编辑器网格实现、自动化结果和真实 API/Web 运行态；验收方未修改正式前端代码，新增 [M4-01 验收报告](<M4：资料编辑器/M4-01 编辑器网格与目录栏/M4-01 验收报告.md>)及本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 141/141、脚本 10/10）；`editor-layout-state.test.js`、`app-shell-grid.test.js` PASS；`git diff --check` PASS。
- 视觉证据：真实 `Transformer` 编辑器完成 1024×768、1280×720、1440×1024、2048×1024 矩阵；Overlay / Protected / Full 断点几何、232px 目录/边注栏和页面级无横向溢出通过；容器查询属性通过。
- 功能检查：双侧栏、仅目录栏、无侧栏、仅边注栏按标准切换顺序均能达到 Overlay / Protected / Full / Compact；但同一“仅目录栏”状态从“无侧栏”进入时错误落入 Overlay，且 Overlay 目录栏遮挡“视图”菜单，鼠标无法关闭目录栏，登记 P1-1、P1-2。
- 可访问性检查：边注栏折叠后焦点落到 `BODY`，未返回重开触发器；折叠/重开按钮缺少 `aria-expanded`、`aria-controls` 等语义，登记 P2-1；运行态控制台 `error` / `warn` / `warning` 为空。
- P0：无。
- P1：2 项；P1-1 模式解析依赖切换历史，P1-2 Overlay 下左目录遮挡编辑器控制路径。
- P2：1 项；P2-1 边注栏折叠/重开缺少 ARIA 语义和焦点恢复。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`REJECTED`
- 下一步：修复 P1-1、P1-2 和 P2-1，补充状态矩阵、Overlay 控制路径与焦点/ARIA 回归；修复后重新验收，暂不解锁 M4-02。

## A-024 M4-01 编辑器网格与目录栏修复后复验

- 日期：2026-08-09
- 对应任务：M4-01 编辑器网格与目录栏
- 执行差异：复验执行侧针对 A-023 的布局状态、Overlay 菜单命中、边注 ARIA 与焦点修复；验收方未修改正式前端代码，新增 [M4-01 修复后复验验收报告](<M4：资料编辑器/M4-01 编辑器网格与目录栏/M4-01 修复后复验验收报告.md>)及本条记录。
- 自动化：`npm test` PASS（API 210/210、Web 141/141、脚本 10/10）；`editor-layout-state.test.js`、`app-shell-grid.test.js`、`shell-controller.test.js`、`library-index-events.test.js` PASS；`git diff --check` PASS。
- 视觉证据：真实 `Transformer` 编辑器完成 1024×768、1280×720、1440×1024、2048×1024 矩阵；Overlay 目录栏已让出文档 Tab/菜单行，Protected / Full 停靠几何、232px 目录/边注栏和页面级无横向溢出通过。
- 功能检查：两条历史路径进入“仅目录栏”均稳定为 Protected；双侧栏、仅目录栏、无侧栏、仅边注栏四种组合通过；Overlay 下可通过“视图 → 隐藏左侧目录区”关闭目录栏，P1-1、P1-2 已关闭。
- 可访问性检查：边注栏 `aria-labelledby`、`aria-hidden`、`aria-expanded`、`aria-controls` 和中文名称通过；折叠后焦点返回重开按钮，重开后焦点返回关闭按钮；运行态控制台 `error` / `warn` / `warning` 为空，P2-1 已关闭。
- P0：无。
- P1：无；A-023 的 P1-1、P1-2 已关闭。
- P2：无；A-023 的 P2-1 已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 结论：`PASS`
- 下一步：解锁 M4-02。

## 后续记录模板

```markdown
## A-xxx 任务名

- 日期：
- 对应任务：
- 执行差异：
- 自动化：
- 视觉证据：
- 功能检查：
- 可访问性检查：
- P0：
- P1：
- P2：
- 偏差记录：
- 结论：PASS / CONDITIONAL PASS / REJECTED / BLOCKED
- 下一步：
```

# M2-04 StatusBarHost 浏览器验证证据

## 运行条件

- 日期：2026-08-06
- 页面：本地正式 API/Web 运行态，真实后端知识库数据。
- API：`http://localhost:3001`
- Web：`http://localhost:3000`
- 前置验收：M2-03 已由验收方记录为 `PASS`。
- 页面控制台：抽查状态下无 `error` / `warning`。

## 视口与截图

| 视口 | 页面/状态 | 实测结果 | 证据 |
| --- | --- | --- | --- |
| 1440×1024 | 资料索引 | 功能槽无保存/视图 action；全局槽显示 `UTF-8` 与“云端已连接”；Footer 高度 32px；无横向溢出 | [索引截图](<浏览器证据/M2-04-1440x1024-资料索引.png>) |
| 1440×1024 | 编辑器 | 功能槽含保存按钮、字数/行数/链接和源码/边注/专注；全局槽无重复 action；无横向溢出 | [编辑器截图](<浏览器证据/M2-04-1440x1024-编辑器.png>) |
| 1280×720 | 资料索引紧凑状态 | Footer 高度 32px；页面 `scrollWidth=1280`；索引无编辑器专属操作 | [紧凑索引截图](<浏览器证据/M2-04-1280x720-资料索引.png>) |
| 1024×768 | 知识库工作域 | 功能槽仅显示功能消息；全局连接状态正常；页面 `scrollWidth=1024` | [知识库截图](<浏览器证据/M2-04-1024x768-知识工作域.png>) |
| 1024×768 | 训练场工作域 | 功能槽无保存/视图 action；全局连接状态正常；页面 `scrollWidth=1024` | [训练场截图](<浏览器证据/M2-04-1024x768-训练工作域.png>) |

## 内容切换核对

| 页面 | 功能状态槽 | 全局状态槽 | 结果 |
| --- | --- | --- | --- |
| 资料索引 | 功能消息；无 `data-save-now`、无 `data-status-action` | `UTF-8`、云端连接状态 | PASS |
| 资料编辑器 | 保存按钮、状态消息、统计和 3 个 `data-status-action` | `UTF-8`、云端连接状态；无功能 action | PASS |
| 知识库 | 工作域功能消息；无编辑器按钮 | `UTF-8`、云端连接状态 | PASS |
| 训练场 | 工作域功能消息；无编辑器按钮 | `UTF-8`、云端连接状态 | PASS |

## 监听器与布局核对

- 编辑器中 `data-status-action="toggle-right-sidebar"` 单击后 `data-right-hidden` 从 `false` 变为 `true`，再次单击恢复 `false`；未出现双重切换。
- Footer 宿主在所有抽查页面高度均为 32px，并位于 ShellBody 之外。
- 1440×1024：`scrollWidth=1440`；1280×720：`scrollWidth=1280`；1024×768：`scrollWidth=1024`。
- 页面控制台 error/warning 日志为空。

## 证据边界

本文件记录执行侧的运行态事实，不替代正式验收报告；浏览器截图均放在当前 M2-04 任务工作区的 `浏览器证据/` 子目录中。

## A-012 返修复验记录

本节记录针对验收方 A-012 REJECTED 的 P1/P2 修复复验；原验收截图保留用于问题历史对照。

| 视口 | 页面/状态 | 实测结果 | 证据 |
| --- | --- | --- | --- |
| 1280×720 | 编辑器 Footer | StatusBarHost 高度 32px；保存按钮与源码/边注/专注按钮均为 28px；按钮 `padding-top/bottom` 均为 `0px`，按钮底部 718.5px，未被 `overflow:hidden` 裁切；`scrollWidth=1280` | [返修编辑器截图](<浏览器证据/M2-04-reverify-01-editor-1280x720.png>) |
| 1280×720 | 编辑器切换知识库 | 功能槽显示“当前工作域：知识库”；不包含“已切换到：Transformer”；无 `data-save-now`、无 `data-status-action`；StatusBarHost 高度 32px；`scrollWidth=1280` | [返修知识库截图](<浏览器证据/M2-04-reverify-02-knowledge-1280x720.png>) |

### 返修复验交互结果

- P1：编辑器状态控件由验收报告记录的 41.796875px 降为 28px，保存及三项状态操作均完整可见。
- P2：从编辑器切换至知识库、训练场或资料索引时，功能槽分别显示当前工作域消息，不再保留旧文档消息。
- 应用控制台日志：`[]`，未发现 `error` / `warning`。

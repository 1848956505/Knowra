# M4-06 编辑器 Footer 与专注模式 P1-3 修复后再次复验验收报告

## 验收结论

- 对应任务：M4-06 编辑器 Footer 与专注模式
- 复验范围：A-034 的 M4-06-P1-3，并回归 A-033 的 P1-1、P1-2、Footer 四档视口与编辑器主路径。
- 执行差异：复验提交 `ab55b66` 的修复实现、[M4-06 A-034 P1-3 修复与复验报告](<M4-06 A-034 P1-3 修复与复验报告.md>)、专项测试和真实 API/Web 运行态；验收方未修改正式前端实现。
- 结论：`PASS`
- 门禁：A-034 P1-3 已关闭，A-033 P1-1、P1-2 回归通过；M4-07 可以解锁。

## 自动化门禁

- `npm run test:api`：PASS，210/210。
- `npm run test:web`：PASS，147/147。
- `node apps/web/test/controllers/editor-render-controller.test.js`：PASS。
- `node apps/web/test/editor-view-state.test.js`：PASS。
- `node apps/web/test/controllers/editor-view-command-controller.test.js`：PASS。
- `git diff --check`：PASS。
- `npm run dev:all` 启动时重新构建 Editor bundle：PASS；API 监听 3001、Web 监听 3003，真实运行态可访问。
- 执行方报告记录脚本测试 10/10 PASS；本次独立复跑该组合命令时审批通道中断，未将中断计作产品失败。核心 API、Web 与本缺陷专项测试均已由验收方独立复跑通过。

## A-034 缺陷关闭情况

### M4-06-P1-3 首页首次打开资料时富文本正文为空：已关闭

- 两个独立新页面会话均从主页进入资料索引；主页阶段 `#milkdown-editor=0`、`.ProseMirror=0`，确认非编辑屏不再提前挂载空 Milkdown Host。
- 会话一首次打开非空资料“测试”：约 300ms 时 `.ProseMirror` 已显示“你好，这首先是一个测试 / 然后呢”，等待 2.6 秒后正文保持一致，Milkdown Host 始终为 1。
- 会话二首次打开非空资料“测试2”：约 250ms 时 `.ProseMirror` 已显示“这个是测试2”，等待 2.6 秒后正文保持一致，Milkdown Host 始终为 1。
- 两次路径均无需切换源码模式即可看到完整正文，未出现空白 Host、延迟清空或重复 Host。
- 当前测试数据的“最近编辑”五条均为空正文，无法使用与 A-034 完全相同的非空最近资料；本次以同一主页摘要态进入资料索引后首次打开非空资料，并结合“非编辑屏不挂载 Host”专项断言验证同一根因链路。

## A-033 回归

- P1-1 焦点交接：进入专注后焦点位于“退出专注模式”；退出后焦点位于“进入专注模式”，回归通过。
- P1-2 阅读语义：阅读模式下 `[contenteditable="true"]=0`；进入、退出专注后仍无可编辑元素，正文持续可见，回归通过。
- Footer 在 1024×720、1280×720、1440×800、2048×1152 四档均可见，源码与专注操作可见；四档 `documentElement.scrollWidth === innerWidth`，无页面级横向溢出。

## 偏差与判定

- P0：无。
- P1：无；A-034 P1-3 已关闭，A-033 P1-1、P1-2 保持关闭。
- P2：无。
- 偏差记录：无新增，无已批准或未批准 D-xxx。
- 最终结论：`PASS`。
- 下一步：解锁 M4-07，继续完整编辑器状态矩阵与最终回归。

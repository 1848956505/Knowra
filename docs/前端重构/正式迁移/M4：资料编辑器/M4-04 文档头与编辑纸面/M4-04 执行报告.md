# M4-04 执行结果

## 结果概述

- 已按任务书迁移面包屑、状态、创建/编辑时间、资料类型、可编辑标题与标签展示。
- 文档头、Milkdown 正文和只读预览共用 `--ink-editor-max-w: 760px` 阅读列宽，并在 EditorMain 内居中。
- 编辑滚动区使用 InkGrid 点阵纸面；1024px Overlay 宽度下切换为更紧凑的内边距和纵向元信息布局。
- 保留原 `#milkdown-editor`、`data-document-title-input`、标题草稿/自动保存事件和编辑区独立滚动宿主。
- 文档纸面样式从 555 行的编辑器综合模块拆入独立 `knowra-inkgrid-editor-paper.css`，没有新增视觉硬编码或第二套 Token 来源。

## 修改文件

- `apps/web/lib/editor/document-head-renderer.js`
- `apps/web/styles/components/knowra-theme-tokens.css`
- `apps/web/styles/components/knowra-inkgrid.css`
- `apps/web/styles/components/knowra-inkgrid-editor.css`
- `apps/web/styles/components/knowra-inkgrid-editor-paper.css`
- `apps/web/test/m4-04-document-paper.test.js`
- `apps/web/test/library-index-renderers.test.js`
- `apps/web/test/knowra-redesign-ui.test.js`
- `apps/web/test/inkgrid-style-entry.test.js`
- `docs/项目结构导航.md`
- `docs/工程变更日志.md`

## 保留的行为契约

- Milkdown Host 仍为 `#milkdown-editor`，没有替换或重新实现编辑器。
- 标题输入继续使用 `data-document-title-input`，输入、Enter、Escape、失焦校验和草稿持久化事件链未改变。
- 正文输入、IME、撤销/重做、Markdown 粘贴、代码块、表格、图片与滚动位置仍由既有 Milkdown/controller 实现负责。
- 正式资料状态、时间、路径、类型与标签均来自当前 note/state，不使用原型静态数据或 `data-hook`。
- 未修改 API、数据模型、缓存恢复、写保护、边注和 Footer 任务范围。

## 新增或修改的测试

- 新增 `m4-04-document-paper.test.js`：覆盖语义面包屑、状态、时间、类型、标题、标签、760px Token、点阵纸面、居中正文/预览及 Milkdown 宿主保留。
- 更新文档头旧结构断言，使其校验 `nav/ol/aria-current/time` 正式语义。
- 更新 InkGrid 聚合入口和 Shell 作用域测试，纳入独立纸面模块。
- 既有输入/IME/快捷键/Markdown 粘贴/代码块/表格/图片/滚动位置测试随 Web 全量门禁继续通过。

## 验证

- `git fetch origin`：当前分支与 `origin/codex/frontend-refactor` 无领先/落后提交。
- `npm run test:web`：PASS，143 passed / 0 failed。
- `npm test`：PASS，API 210 / Web 143 / scripts 10。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS。
- 浏览器视口与状态：
  - 2048×1024：Full；文档头与 ProseMirror 均为 760px；页面宽度 2048/2048；Milkdown Host 1 个。
  - 1440×1024：Protected；文档头与 ProseMirror 均为 760px；页面宽度 1440/1440；编辑滚动区 1000/1000。
  - 1024×768：Overlay；纸面按可用宽度收缩为约 741.6px；页面宽度 1024/1024；编辑滚动区可独立滚动到 `scrollTop≈184.8`。
- 浏览器交互：长标题可见且保持单行承载；标题追加临时文本后按 Escape 恢复原值；没有留下资料变更。
- 控制台：`error` / `warn` / `warning` 均为空。
- 浏览器证据：
  - `浏览器证据/M4-04-1440x1024.png`
  - `浏览器证据/M4-04-1024x768.png`

## 与目标视觉的已知差异

- 正式数据没有标签的资料显示“暂无标签”，不虚构原型中的示例标签。
- Protected / Overlay 模式的边注栏继续按 M4-01 已验收契约覆盖 EditorMain；纸面不为覆盖层再次缩窄。

## 风险与待决问题

- 本次未发现需要新增决策或偏差记录的问题。
- 当前版本号保持 `2.12.0`；版本推进属于 M6-02 文档与版本维护任务，本任务不提前修改三个 `package.json`。

## 建议验收范围

- 复核 2048、1440、1024 三档的纸面轴线、长面包屑/标题和边注覆盖关系。
- 复核标题输入/IME/Escape、正文输入/撤销/粘贴、代码块、表格、图片和跨 Tab 滚动恢复。
- 复核 `#milkdown-editor` 唯一性、控制台空错误及页面级无横向溢出。

本报告仅提交执行结果，不代表规划与验收方已经给出 M4-04 `PASS`。

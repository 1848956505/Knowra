# M4-04 文档头与编辑纸面验收报告

## 验收结论

- 对应任务：M4-04 文档头与编辑纸面
- 执行差异：审查 `bb4abf1` 之上当前未提交工作区的文档头渲染、760px 阅读列、独立纸面样式模块、专项测试、执行报告和真实 API/Web 运行态；验收方未修改正式前端实现。
- 结论：`PASS`
- 解锁：M4-05 资料边注。

## 自动化门禁

- `npm run test:api`：PASS，210/210。
- `npm run test:web`：PASS，143/143。
- `npm run test:scripts`：PASS，10/10。首次沙箱运行因 Node 子进程统一报 `spawn EPERM`，在允许子进程的环境复跑后全部通过，未出现断言失败。
- `node apps/web/test/m4-04-document-paper.test.js`：PASS。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS；仅有工作区换行转换提示，无空白错误。

## 静态结构与范围

- `#milkdown-editor` 保持唯一且 `renderRichEditorHost()` 的正式宿主契约未改变。
- 标题继续使用 `data-document-title-input`；正文编辑器控制器、API、数据模型、保存与离开保护均未改动。
- 面包屑使用 `nav`、`ol` 和 `aria-current="page"`；创建、编辑时间使用 `time[datetime]`；状态包含文本，不以颜色作为唯一提示。
- 状态、时间、类型、路径、标题和标签均来自正式 note/state；未发现原型 `data-hook`、CDN 或第二套 Token 来源。
- 纸面样式从综合编辑器模块拆入 `knowra-inkgrid-editor-paper.css`，由正式 InkGrid 入口单向导入。

## 视口与视觉证据

| 视口 | 布局 | 文档头 / ProseMirror | 页面宽度 | 结果 |
| --- | --- | --- | --- | --- |
| 2048×1024 | Full | 760px / 760px | 2048 / 2048 | PASS |
| 1440×1024 | Protected | 760px / 760px | 1440 / 1440 | PASS |
| 1280×720 | Overlay | 760px / 760px | 1280 / 1280 | PASS |
| 1024×768 | Overlay | 约 741.6px / 约 741.6px | 1024 / 1024 | PASS |

- 复核现有 `M4-04-1440x1024.png`、`M4-04-1024x768.png` 原件；长面包屑、长标题、状态、时间、类型与无标签真实空态可见。
- 四档运行态均无页面级横向滚动；编辑纸面在窄容器按可用宽度收缩，点阵背景和独立纵向滚动宿主保持生效。
- 四档均只有一个 `#milkdown-editor`；控制台 `error` / `warn` / `warning` 为空。

## 功能检查

- 标题输入追加临时中文后按 Escape 恢复原值，未留下资料变更。
- 正文中文输入后 Undo 回到空稿；Markdown 粘贴真实生成 H2 与列表，再由一次 Undo 清除。
- 含 fenced code、Markdown 表格和本地图片引用的粘贴真实生成 1 个 Milkdown code block、表格和图片节点，再由一次 Undo 清除。
- 既有 `editor-scroll-controller.test.js` 覆盖保存、动画帧恢复、缺失宿主保护和大纲跳转取消恢复；Web 全量门禁保持通过。
- 输入、历史、Markdown 粘贴、代码块、表格、图片与滚动控制仍走既有 Milkdown/controller 链路，未被 M4-04 视觉迁移替换。

## 可访问性检查

- 文档路径具有中文导航名称，当前路径段使用 `aria-current="page"`。
- 标题为原生文本框且具有“资料标题”可访问名称；状态点为装饰元素并隐藏于辅助技术。
- 标题 Escape 恢复路径通过；正文仍为真实可编辑 Milkdown 文本框。

## 发现

- P0：无。
- P1：无。
- P2：无。
- 偏差记录：无新增，无已批准或未批准 D-xxx。

## 下一步

- M4-04 `PASS`，解锁 M4-05 资料边注。
- M4-07 仍需按完整编辑器状态矩阵执行最终视觉与回归验收。

# M4-07 编辑器视觉与回归执行报告

## 1. 执行结论

- 状态：执行完成，等待规划与验收方复验。
- 解锁依据：`06-验收记录.md` A-035 判定 M4-06 `PASS` 并明确解锁 M4-07。
- 范围：执行 M4 完整编辑器状态矩阵、功能与可访问性回归；未修改正式迁移计划、契约或验收记录。
- 执行中发现并修复 1 项焦点回归：隐藏标签溢出菜单按 Escape 关闭后焦点落到 `BODY`。

## 2. 修复内容

`tab/overflow-controller.js` 的 `close()` 新增可选 `restoreFocus`，全局 Escape 关闭链以 `{ restoreFocus: true }` 调用。菜单关闭后将 `aria-expanded` 与 `data-open` 设为 `false`，并把焦点交还当前可见的溢出按钮。鼠标外部点击仍沿用不强制抢焦点的关闭路径。

## 3. 视口与状态矩阵

| 证据 | 数据与页面状态 | 面板 / 布局状态 | 结果 |
| --- | --- | --- | --- |
| `01-2048x1024-双侧栏.png` | 非空资料“测试”，Milkdown Host 1 | 目录 + 边注，Full | 无页面横向溢出 |
| `02-2048x1024-仅目录栏.png` | 同输入 | 仅目录栏 | 无页面横向溢出 |
| `03-2048x1024-无侧栏.png` | 同输入 | 无目录、无边注 | 无页面横向溢出 |
| `04-2048x1024-仅边注栏.png` | 同输入 | 仅 232px 边注栏 | 无页面横向溢出 |
| `05-1440x1024-双侧栏.png` | 同输入 | 目录 + 边注，Protected | 无菜单竖排、无页面横向溢出 |
| `06-1280x720-源码分栏.png` | 同输入 | Overlay + 源码/预览分栏 | 两侧源码区域可见，Footer 32px |
| `07-1024x768-阅读态专注.png` | 同输入 | Compact + 阅读态专注 | `contenteditable=true` 为 0，Footer 32px |
| `08-2048x1024-十标签与长标题.png` | 10 个打开标签，含长中文标题 | Full | 活动标签可见，无横向溢出 |
| `09-1280x720-十标签溢出.png` | 同 10 标签 | Overlay，隐藏标签入口可见 | 活动标签可见，无横向溢出 |
| `10-1280x720-溢出菜单Escape焦点返回.png` | 同 10 标签 | Escape 关闭溢出菜单 | 焦点返回溢出按钮，`aria-expanded=false` |

所有浏览器状态下 `.milkdown-host` 保持唯一；真实非空资料首次打开立即显示正文。浏览器控制台 `error` / `warn` / `warning` 为 0。

## 4. 功能与可访问性回归

- 输入、保存、离开保护、菜单命令、源码、标签、附件、标注、大纲、专注等正式链路由完整 Web 测试与 M4-01 至 M4-06 已验收专项共同覆盖。
- 本轮真实验证：主页加载、资料索引进入、非空资料首次打开、四种侧栏组合、源码分栏、阅读态专注、10 标签及溢出菜单、边注 Tab 键盘切换。
- 资料边注“信息”按 ArrowRight 后“大纲”同时成为激活项与焦点项。
- 阅读态进入专注后正文保持只读，焦点位于“退出专注模式”；退出/源码/侧栏偏好沿既有 M4-06 契约恢复。
- 当前真实数据没有多标签和长文件名附件；对应 renderer、事件、controller 与 M4-05 验收证据随 148 项 Web 回归保持通过，本轮未制造持久化测试数据。

## 5. 自动化门禁

- `node apps/web/test/controllers/tab-overflow-controller.test.js`：PASS。
- `node apps/web/test/events/document-keyboard-events.test.js`：PASS。
- `npm test`：PASS，API 210/210、Web 148/148、scripts 10/10。
- `npm run build:editor-bundle -w @study-accelerator/web`：PASS。
- `git diff --check`：PASS；仅行尾转换提示，无空白错误。

## 6. 变更文件

- `apps/web/src/controllers/tab/overflow-controller.js`
- `apps/web/lib/events/document-keyboard-events.js`
- `apps/web/test/controllers/tab-overflow-controller.test.js`
- `apps/web/test/events/document-keyboard-events.test.js`
- `docs/前端重构/正式迁移/M4：资料编辑器/M4-07 编辑器视觉与回归/浏览器证据/*`

## 7. 交付说明

- 本报告不宣称正式 `PASS`；结论由验收方写入 `06-验收记录.md`。
- 本轮远端 `git fetch` 的授权通道异常中断；本地 `origin/main` 快照显示当前分支领先 29、落后 0，该同步信息可能不是远端最新状态。

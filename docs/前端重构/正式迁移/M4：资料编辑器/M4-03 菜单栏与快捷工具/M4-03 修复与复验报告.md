# M4-03 修复与复验报告

## 结果概述

- 修复范围：A-028 的 P1-1、P2-1、P2-2。
- 执行侧状态：修复已完成，待规划与验收方复验；本报告不自行修改 A-028 的 `REJECTED` 历史结论。

## 修复内容

1. 打开“文件 / 段落 / 编辑 / 格式 / 视图 / 更多”后，重绘完成即聚焦第一个可用 `menuitem`，键盘可继续访问命令。
2. Escape 关闭已打开菜单后，焦点返回对应触发器；外部点击和命令执行关闭不强制抢占焦点。
3. Full ↔ Compact/Protected/Overlay 切换时，仅在原焦点位于菜单栏时进行交接：收纳后进入“更多”首项，返回 Full 后聚焦可见的“文件”触发器。
4. `aria-controls` 只在对应弹层已渲染时输出，关闭态不再指向不存在的 DOM 目标。
5. 清理 `docs/前端重构/正式迁移/README.md` 尾部多余空行，恢复全局 `git diff --check` 门禁。

## 修改文件

- `apps/web/lib/editor/menu-focus.js`
- `apps/web/lib/editor/menu-renderers.js`
- `apps/web/lib/events/menu-events.js`
- `apps/web/lib/events/document-keyboard-events.js`
- `apps/web/src/controllers/editor/render-controller.js`
- `apps/web/src/controllers/editor/commands/menu-state-controller.js`
- `apps/web/src/controllers/shell-controller.js`
- M4-03 相关渲染、事件、键盘和布局状态测试。

## 自动化门禁

- `node apps/web/test/editor-menu-renderers.test.js`：PASS。
- `node apps/web/test/events/menu-events.test.js`：PASS。
- `node apps/web/test/events/document-keyboard-events.test.js`：PASS。
- `node apps/web/test/editor-layout-state.test.js`：PASS。
- `npm run test:web`：PASS（142/142）。
- `npm test`：PASS（API 210/210、Web 142/142、脚本 10/10）。
- `git diff --check`：PASS。

## 真实浏览器 DOM 复验

使用正式 API/Web 和真实资料 `CLIP`：

- 1280×720 Overlay：关闭态“更多”的 `aria-controls` 为 `null`，目标不存在；打开后 `aria-controls="editor-menu-more"` 与目标同时存在。
- 打开“更多”后，`document.activeElement` 为“新建笔记” `menuitem`，弹层仍包含 43 个正式命令。
- 在菜单项上按 Escape 后，弹层关闭，`document.activeElement` 为 `data-editor-menu-toggle="more"` 触发器。
- Overlay 打开“更多”后切至 2048×1024 Full：焦点落到可见“文件”触发器；在 Full 打开“段落”后切至 1600×720 Compact：自动迁移到“更多”且首项获焦。
- 两档切换后页面无横向溢出，控制台 `error` / `warn` / `warning` 为空。

## 待验收方复核

- 复核 A-028 P1-1 的打开/Escape/布局切换焦点连续性。
- 复核 A-028 P2-1 的关闭态 ARIA 关联。
- 复核 A-028 P2-2 的全局差异格式门禁。

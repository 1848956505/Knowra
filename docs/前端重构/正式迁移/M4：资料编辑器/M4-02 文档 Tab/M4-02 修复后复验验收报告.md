# M4-02「文档 Tab」修复后复验验收报告

## 结论

- 日期：2026-08-09
- 对应任务：M4-02 文档 Tab
- 验收基线：`codex/frontend-refactor`，基于 `8a54b83` 的当前未提交 A-026 修复工作区；复验期间验收方未修改正式前端代码。
- 复验范围：重新检查 9+ 个真实资料标签、长标题、活动标签可见、溢出菜单、关闭/右键/Escape、脏状态、Tab/tabpanel 语义、焦点连续性及四档编辑器视口。
- 结论：`PASS`
- 结论依据：A-026 的 P1-1、P1-2、P2-1 均已关闭，未发现新的 P0/P1/P2。

## 自动化与运行态证据

- `npm test`：通过；API 210/210、Web 142/142、脚本 10/10。
- M4-02 相关专项测试通过：`m4-02-tab-ui.test.js`、`tab-renderers.test.js`、`note-tab-events.test.js`、`tab-controller.test.js`、`editor-draft-controller.test.js`。
- `git diff --check`：通过。
- 真实 API/Web：`http://localhost:3001` 与 `http://localhost:3000` 可用；页面控制台 `error` / `warn` / `warning` 均为空。

## 真实浏览器复验

使用真实资料打开 12 个文档标签，完成以下矩阵：

| 视口 | 可见 Tab | 溢出 Tab | 编辑区 | 页面级横向滚动 |
| --- | ---: | ---: | --- | --- |
| 2048×1024 | 12 | 0 | `x=440, width=1376` | 无，`scrollWidth=2048` |
| 1440×1024 | 5 | 7 | `x=440, width=1000` | 无，`scrollWidth=1440` |
| 1280×720 | 6 | 6 | `x=208, width=1072` | 无，`scrollWidth=1280` |
| 1024×768 | 6 | 6 | `x=208, width=816` | 无，`scrollWidth=1024` |

### A-026 P1-1：溢出菜单切换隐藏标签

- 1280×720 下打开“显示其余 5 个标签页”，点击隐藏项 `CLIP`。
- 活动 Tab 切换为 `CLIP`，隐藏项进入可见 Tab，菜单关闭，焦点落在 `role="tab"` 的 `CLIP` 按钮。
- 溢出按钮已移出 `#note-tabs[role="tablist"]`，菜单为 `role="menu"`，隐藏项为 `role="menuitemradio"`，边注遮挡层在菜单打开时不命中指针。

### A-026 P1-2：未保存修改 Tab 脏点

- 将真实资料 `CLIP` 标题临时修改为 `验收临时标题-M4-02-复验`，立即读取运行态。
- 保存状态为 `pending`；活动 Tab 为 `data-dirty="true"`，脏点显示 `●`，可访问名称包含“有未保存修改”。
- 复验后已恢复标题 `CLIP`，确认状态回到 `saved`，未保留临时修改。

### A-026 P2-1：Tab/tabpanel 语义与焦点连续性

- 可见 Tab 均有唯一稳定 `id`，仅活动 Tab 为 `tabindex="0"`，其余为 `tabindex="-1"`；外层拖拽容器为 `role="presentation"`。
- `#editor-scroll-region` 保持 `role="tabpanel"`，并通过 `aria-labelledby` 回链当前 Tab；当前 Tab 的 `aria-controls` 指向该面板。
- 点击可见 Tab 后焦点保持在活动 Tab；`ArrowRight`、`Home`、`End` 均能切换活动 Tab、同步焦点和面板关系。
- 关闭活动 Tab 后焦点恢复到相邻活动 Tab；右键菜单可打开，Escape 可关闭并恢复焦点。

### 其他验收项

- 长标题 `A股短线 ChatGPT 使用模板` 在 1280×720 下保持可见；标签单行 `ellipsis` 生效，文本未撑破容器。
- Tab 文件图标、选择、关闭、右键菜单、Escape、拖拽状态与重排规则保持通过；拖拽行为由事件与纯规则回归覆盖。

## 缺陷与后续

- P0：无。
- P1：无；A-026 的 P1-1、P1-2 已关闭。
- P2：无；A-026 的 P2-1 已关闭。
- 偏差记录：无新增、无已批准或未批准 D-xxx。
- 下一步：解锁 M4-03 菜单栏与快捷工具。

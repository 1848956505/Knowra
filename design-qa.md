# V4-07 笔记标题区与紧凑侧栏头 Design QA

## 比较目标

- 唯一视觉真源：`docs/前端重构/V4/V4-00.5/印格/印格-demo-笔记页面.html`
- 侧栏头参考：`/var/folders/pb/fc84ns390y39c478dnyv74fm0000gn/T/codex-clipboard-860a6b48-6d65-4d50-b990-8fddc3f96541.png`
- 标题区参考：`/var/folders/pb/fc84ns390y39c478dnyv74fm0000gn/T/codex-clipboard-737d53d1-3bb1-4168-8a8d-43347ac689c9.png`
- 浏览器实现：`http://127.0.0.1:5173/#/materials/notes/note-item-1781422707366`
- 桌面实现截图：`/private/tmp/knowra-editor-title-matched.png`
- 窄屏实现截图：`/private/tmp/knowra-editor-title-mobile.png`
- 源图与实现聚焦组合：`/private/tmp/knowra-title-comparison-final.png`

## 视口、像素与归一化

- 桌面 CSS 视口：`1280 × 720`，`devicePixelRatio = 2`；浏览器后端导出 `1280 × 720` JPEG。
- 窄屏 CSS 视口：`390 × 843`；文档根 `scrollWidth = 390`，无页面级横向溢出。
- 标题源图：`1653 × 612` PNG；侧栏源图：`961 × 162` PNG。
- 组合图先从桌面实现截图裁取同一标题/工具栏区域，再将源图与实现图分别等比归一化到 `1200px` 宽并纵向拼接。源图与实现来自不同窗口裁切，不把外围画布和数据内容差异当作视觉漂移。

## 状态与主要交互

- 初始态：侧栏顶部栏与文档标签栏实测均为 `36px`；侧栏正文密度和独立滚动未改变。
- 标题态：标题使用真实 `textarea` 编辑但保留一级标题语义；长标题自然换为两行，聚焦显示视觉真源中的蓝色底线。
- 键盘与保存：Enter 提交并去除首尾空白，Esc 恢复服务端标题；失焦走同一保存函数。
- 校验与恢复：空标题被恢复为原标题，并通过关联字段的 `role="alert"` 就地说明；API 失败保留草稿并提示重新编辑或按 Enter 重试。
- 数据与同步：复用 `renameNote → updateNote`，成功后由既有 workspace 刷新同步标签页、目录和检查器；离线/缓存模式标题只读。
- 浏览器未写入验收垃圾数据：长标题输入通过 Esc 撤销；空标题和同名规范化路径未调用后端 mutation。
- 控制台：`0` 条 error / warning。

## 全视图比较

请求范围内的信息层级一致：紧凑标签栏下是点阵画布与白纸；文档标识、封面、大标题、状态元信息、悬浮工具栏和正文按视觉真源的顺序与留白出现。正式 AppShell 左轨/侧栏属于产品运行结构，参考标题图为编辑纸张裁切，因此外围差异属于预期。

## 聚焦区域比较

- 字体与排版：标题沿用视觉真源的 `clamp(30px, 4.2vw, 44px)`、`1.18` 行高、紧字距与粗体层级；kicker 和元信息使用 mono 字体，长英文标题在同一区域形成两行。
- 间距与布局：封面 `72 × 98px`、标题列 `20px` 间隙、kicker `92px` 左偏移、元信息和工具栏纵向节奏与源代码一致；移动端缩为 `54 × 74px` 并保持可读。
- 颜色与 Token：蓝色强调、橙色 draft、纸张/点阵/边框及硬阴影全部引用现有 `--ink-*`、`--shadow-*` 和新增的视觉真源 `--tab-size` Token。
- 图像与资产：本轮没有照片或位图资产；文件封面直接复现视觉真源本身的 CSS 图形语言，工具栏/侧栏图标继续复用项目现有图标系统，没有引入 emoji、临时 SVG 或占位图。
- 文案与内容：固定文案与视觉真源一致；标题、文件夹与日期为真实动态数据。错误文案同时说明原因和恢复方式。
- 可访问性：标题有可访问名称、字段错误关联、可见键盘焦点和 Enter/Esc 路径；只读状态使用原生 `readOnly`；390px 下无横向溢出。

## Findings

- 无可执行的 P0 / P1 / P2 视觉、交互或无障碍问题。
- P3：聚焦组合图因源图与浏览器截图裁切比例不同做了等宽归一化，适合判断结构、层级、换行和节奏，不作为逐像素回归基线。

## 比较历史

1. 首轮实现使用静态 `<h1>`，标题区只有展示能力，无法接入正式重命名链路。
2. 修复：拆出 `EditorDocumentHeader`，增加标题编辑、Enter/失焦保存、Esc 撤销、空值恢复、失败重试提示和离线只读。
3. 规范复审发现输入控件自身持有焦点阴影且缺少表单元数据。
4. 修复：焦点/错误线转由复合标题外壳持有；补充 `name`、`autocomplete="off"`、`spellcheck="false"`，并让错误文案包含明确恢复路径。
5. 复验：`/private/tmp/knowra-title-comparison-final.png` 显示标题、封面、元信息与工具栏结构匹配；390px 截图无横向溢出；控制台零告警。

## Implementation Checklist

- [x] 侧栏顶部栏与标签栏统一为 36px，正文和滚动边界不变。
- [x] 标题区按唯一视觉真源还原并支持长标题换行。
- [x] 标题编辑复用正式后端能力，无新增 API 或跨层调用。
- [x] Enter、失焦、Esc、空值、失败和只读状态有测试覆盖。
- [x] V4 类型、边界与 124 项测试通过；全仓 504 项测试通过。
- [x] 桌面/390px 浏览器视觉、交互与控制台检查通过。

final result: passed
